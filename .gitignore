# Вендорные бинарники (восстанавливаются отдельно, велики для git)
vendor/models/
vendor/ort/
vendor/opencv.js
vendor/pdfjs/
vendor/dxf-parser/
vendor/dwgdxf/

# Экспериментальные артефакты и образцы планов
seg-preview/
chertez/
.tmp-*/
outputs/

# Фирменные каталоги VIMAR в PDF (~155 МБ) — справочник для разработки, карта страниц
# лежит в docs/каталоги-vimar-pdf.md. Сами файлы в git не тащим.
*.pdf

# IDE
.idea/

# npm нужен для автотестов и тулинга (пересборка каталога), рантайм-зависимостей нет
node_modules/

# Личные настройки Claude Code — не для команды
.claude/settings.local.json
CLAUDE.local.md
