---
name: bitrix-map
description: Полная карта проекта — все инфоблоки, highload-блоки, кастомные модули, компоненты и интеграции с их ID и назначением. Используй при работе с CIBlockElement/CIBlockSection, HL-блоками, при добавлении новых сущностей или когда нужно найти, где хранятся конкретные данные проекта.
---

# Карта проекта <название>

> Этот файл не грузится в контекст автоматически каждую сессию — Claude
> подгружает его сам, когда задача касается инфоблоков/HL-блоков/интеграций.
> Поэтому здесь можно писать подробно, не боясь раздуть CLAUDE.md.
> Актуализируйте таблицы вручную или попросите Claude один раз собрать их
> из админки/БД и сохранить сюда.
>
> Разделение с соседним skill `1c-bitrix`: там — общие знания платформы
> (как работает D7 API, инфоблоки, безопасность — актуально для любого
> Bitrix-проекта). Здесь — данные именно этого проекта (какие конкретно
> ID инфоблоков и модули есть у вас). Заполняйте оба, они не дублируют
> друг друга.

## Инфоблоки
| ID | Символьный код | Тип инфоблока | Назначение | Где используется в коде |
|----|-----------------|----------------|------------|---------------------------|
| `<1>` | `<news>` | `<content>` | `<Новости компании>` | `<local/components/.../news>` |

## Highload-блоки
| ID | Имя таблицы (TABLE_NAME) | Назначение | Где используется в коде |
|----|----------------------------|------------|---------------------------|
| `<1>` | `<hl_partners>` | `<Список партнёров>` | `<local/modules/...>` |

## Кастомные модули
| Код модуля | Путь | Назначение |
|------------|------|------------|
| `<vendor.module>` | `/local/modules/<vendor.module>/` | `<...>` |

## Кастомные компоненты
| Компонент | Путь | Назначение |
|-----------|------|------------|
| `<vendor:component>` | `/local/components/<vendor>/<component>/` | `<...>` |

## Внешние интеграции
- REST API Битрикс24 / входящие вебхуки: `<...>`
- Исходящие вебхуки / обработчики событий Б24: `<...>`
- Сторонние API (CRM, платёжные системы, доставка): `<...>`

## Прочее
- `<особенности мультисайтовости, если есть>`
- `<особая структура торгового каталога / SKU, если e-commerce>`
