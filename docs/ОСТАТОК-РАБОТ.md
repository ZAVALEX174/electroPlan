# ElectroPlan — остаток работ (единый список)

> **Это главный список «что ещё не сделано».** На вопрос «что осталось / что дальше» —
> отвечать ПО ЭТОМУ ФАЙЛУ, заново всё не перепроверять.
> Сделал пункт — отметь `[x]`, дату и коммит. Появилось новое требование — допиши сюда.
>
> Составлен 24.09.2026 полной сверкой `docs/итоги-встречи-2026-08-24.md` (все разделы, не только
> §4 и §1.3) с кодом. Состояние на момент сверки: `main` = `f9ba935`, 1391 тест.
> Раньше сверяли только §4 и §1.3 — поэтому пласт §1 (ответы по опроснику) всплыл лишь сейчас.


## ▶ СТАРТ СЛЕДУЮЩЕЙ СЕССИИ (записано 26.09)

- `main` = последний коммит 26.09, **1477 тестов зелёные**, всё запушено, рабочее дерево чистое.
- За 23–26.09 выложены: накладка нового поста из комнаты, **А2** скидка на позиции, **А3** НДС три режима,
  **А4** автономер КП, **Б1** счётчик одиночных элементов, **Б2** «общая сборка / взрыв-схема».
- **Начать с мелочей В1 → В2 → В3 → В9** (каждая — одна фраза результата владельцу, «да», ep-dev, проверка).
  Затем по порядку внизу файла. Перед каждым пунктом — фраза результата словами владельца и ждать «да».
- Ждём ответов заказчика — раздел Е (письмо `docs/вопросы-заказчику-2026-09-22.txt`, не известно, отправлено ли).
- Процесс: код пишет ep-dev; ep-verify + ep-adversary параллельно; все мутации перепроверять лично в изолированной
  копии (scratchpad/mut20 + скрипты run-*.js); смотреть глазами — headless-скрипты scratchpad/deck/*-shot.mjs
  (свой node-сервер на :8431, т.к. python http.server роняет загрузку). Пуш в main = сразу виден заказчику.

---

## А. Деньги и документы — наша работа, не ждёт заказчика (короткие, видны в КП сразу)

- [ ] ⏸ **А1. Наценка в КП.** (владелец 24.09: пропустить; ждёт ответа — наценка видна клиенту строкой или в цене) §1.2: нужны инструменты «Скидка» и «Наценка», меняющие КП. Сейчас есть
      только «Скидка, %» (`index.html` `discountInput`); наценки нет.
- [x] **А2. Скидка на отдельные позиции.** ✅ 26.09: поле «скидка, %» у строки спецификации, пометка в КП. §1.2: «на весь объём, с возможностью правки отдельных
      позиций». Сейчас скидка только на всё оборудование (`js/estimate.js`, `discountPercent`).
- [x] **А3. НДС — три режима.** ✅ 24.09: селектор «Не учитывать / Выделить в стоимости / Начислить сверху», панель и КП. §1.2: «Не учитывать» / «Выделить в стоимости» / «Начислить сверху
      с %». Сейчас только галочка «Включать НДС в КП» + процент — режима «выделить в стоимости» нет.
- [x] **А4. Автонумерация КП `EPG-2026-0001`.** ✅ 24.09: при первой печати КП, закрепляется за проектом. §1.3, подтверждено. Сейчас поле «Номер КП»
      заполняется руками (`docNumber`).
- [ ] **А5. Реквизиты с галочками полей.** §1.3: выбор отображаемых полей своих реквизитов
      (минимум ФИО и контакты, опционально юрлицо, адрес…) и реквизитов заказчика. Сейчас поля
      есть, выбора нет.
- [ ] **А6. Спецификация в Excel.** §1.2: «приоритет PDF, спецификацию можно в Excel». Сейчас только PDF.
- [ ] **А7. Запоминать скидку/настройки по клиенту.** §1.2: «у Иванова постоянная скидка 10%».
- [ ] **А8. Два документа «Расчёт» и «КП».** §1.3: «нужны два ключевых документа». Уточнить у
      заказчика, чем «Расчёт» отличается от нынешних КП + спецификации.

## Б. План и редактор — наша работа

- [x] **Б1. Убрать одиночные элементы с плана.** ✅ 26.09: счётчик «Элементов» виден только если они есть (старые проекты; не удаляются — они в смете). Мёртвый код размещения оставлен. §4.8: «элементы только в блоках будут. Отдельно
      кнопку нет смысла размещать». Всё ставится только постами.
      Проверка 24.09: поставить одиночный элемент из интерфейса УЖЕ нельзя (путь state.pending type device
      никто не взводит). Остатки: счётчик «Элементов: N» в строке статуса, мёртвый код размещения, одиночные
      элементы старых проектов по-прежнему рисуются и считаются.
- [x] **Б2. Выбор вида: «общая сборка» / «взрыв-схема».** ✅ 26.09: «Вид поста в листе монтажника» в «Настройке состава КП». §4.9. Взрыв-схема есть, переключателя нет.
- [ ] **Б3. Поворот плана.** §1.4: «повернуть нужно обязательно».
- [ ] **Б4. Отмена действия (undo/redo).** §1.4: «да». В коде прямо записано «undo в приложении нет»
      (`js/app.js` у `clearPlan`).
- [ ] **Б5. Групповое выделение.** §1.4: «обязательно для отелей и коммерческих объектов».
- [ ] **Б6. Этажи.** §1.4: «этажи нужны».
- [ ] **Б7. Экспликация по комнатам.** §1.4: «да, если делаем комнаты».
- [ ] **Б8. Вертикальные / горизонтальные рамки.** §1.4: «обсудить» (в некоторых коллекциях это
      разные артикулы) — сначала вопрос заказчику.

## В. Мелочи, найденные 21–24.09

- [ ] **В1.** При «Разместить» клик ровно по табличке комнаты не ставит пост — табличка
      перехватывает клик (`.room-label`, `pointer-events:auto`). Нужно кликать рядом.
- [ ] **В2.** Плашка «Под эту комнату накладок нет»: совет «смените отбор» звучит дважды; у нового
      поста лишняя фраза «Механизмы поста сохранены».
- [ ] **В3.** В виде «С картинками» пока стандарт не выбран, слово «Стандарт» нигде не написано —
      владелец не нашёл его (24.09). Предложено: строка «Стандарт: не задан» над «Подобрать накладку».
- [ ] **В4.** Хинт конструктора «сузили: …» при непустом отборе не называет стандарт (при пустом —
      называет, сделано 24.09).
- [ ] **В5.** Многосерийная накладка при подмене сохраняет только первую серию (12 из 1631).
- [ ] **В6.** Печать в КП стоит справа от подписи, а не внахлёст.
- [ ] **В7.** КП, режим НДС «Начислить сверху»: «Итого без НДС» + «НДС» может разойтись с «Итого с НДС»
      на 0,01 — каждое число округляется отдельно (было и до 24.09; пример: 1 250,01 + 250,00 ≠ 1 500,02).
- [ ] **В8.** Автономер КП: выдаётся ДО проверки «КП пуст» — нажатие КП на пустом проекте закрепляет
      номер за ним; вписанный руками номер формата EPG-ГГГГ-NNNN (в т.ч. опечатка) поднимает счётчик года.
- [ ] **В9.** Панель «Стоимость проекта»: при личных скидках строка «Скидка −6,73 € (10%)» — сумма
      с учётом личных, а в скобках общий процент (выглядит противоречием; в КП верно — «общая 10%, у отмеченных
      позиций своя»). Узкое поле «скидка, %» в спецификации обрезает знак «%».

## Г. Подсветка клавиш

- [ ] **Г1. Подсветка, часть B** — опция «считать подсветку», цвет, всем выключателям, в состав и
      смету (ключевое слово ПОДСВЕТКА-B, раздел в HANDOFF от 09.09). Правило для NEVE и Eikon Flat
      ждёт ответа заказчика (вопрос 3 письма от 22.09): 9,60 € за светодиод.

## Д. Большой этап «сервис на продажу» — обсудить приоритеты с заказчиком отдельно

- [ ] **Д1.** Личные кабинеты, многопользовательский режим (§1.1).
- [ ] **Д2.** Уровни доступа демо / менеджер / профи, покупка доступа к брендам (§1.1); логотип и
      подпись по тарифу (§1.3).
- [ ] **Д3.** Версии проекта и история версий КП с удалением лишнего; курс евро — версионировать
      проект, а не фиксировать (§1.2, §1.5).
- [ ] **Д4.** Обязательные поля проекта (адрес, заказчик, план) и проверка уникальности КП (§1.5).
- [ ] **Д5.** Сравнение спецификаций разных компаний по одному объекту (§1.5).
- [ ] **Д6.** Прайсы в других валютах и с другой наценкой — под будущие бренды (§1.2).

## Е. Ждёт заказчика (письмо `docs/вопросы-заказчику-2026-09-22.txt`)

- [ ] **Е1.** Код функции изделия — без него не заменить клавиши при смене серии. Опечатка 19105.B.
- [ ] **Е2.** Фото 698 артикулов (`docs/письмо-заказчику-список-без-фото.xlsx`).
- [ ] **Е3.** Подсветка NEVE / Eikon Flat; звонковые кнопки — 220 В или 00935 (12–24 В).
- [ ] **Е4.** Аксессуары: 02973, 02974, 02965.1 + таблица «механизм → аксессуар» (§4.5).
- [ ] **Е5.** Логотип EPG в векторе, текст «Соглашения сторон», формат номера EPG-2026-0001.
- [ ] **Е6.** Уровни доступа: типы пользователей; коды 2 и 4 в «Группе доступа».
- [ ] **Е7.** Прайс со статусами (в наличии / под заказ / снят, замены) и номенклатура LINEA.
- [ ] **Е8.** Решение: немецкий стандарт без накладки на 3 модуля — по умолчанию 2 или 4 модуля
      (сейчас 2).

## З. Отложено НАМИ — хвосты и края, о которых нельзя забыть

- [ ] **З1.** А4 автономер и Б1 — ep-verify/ep-adversary НЕ запускались (лимит); края проверены вручную и мутациями.
      При случае прогнать поиск краёв по обоим.
- [ ] **З2.** Б1: в app.js остался мёртвый код размещения одиночного элемента (state.pending type "device", ветка
      в addPending) и одиночные элементы старых проектов рисуются на плане. Удалять код — только вместе с решением,
      что делать с такими элементами в старых проектах (сейчас: видны, считаются, удаляются вручную).
- [ ] **З3.** А2: пост со своей скидкой, равной общей (10 и 10), идёт ОТДЕЛЬНОЙ строкой от постов без своей скидки
      (значение задано явно и переживёт смену общей). Если владелец хочет сливать — решение.
- [ ] **З4.** А2: при правке скидки, которая сливает две строки в одну, фокус Tab может уехать на соседнюю строку
      (восстановление по индексу строки). Редкий случай.
- [ ] **З5.** А3: режим НДС для НОВОГО проекта — «Начислить сверху» (как было). Иной дефолт — решение владельца.
- [ ] **З6.** Новый пост в немецкой комнате без 3-модульных накладок берёт МЕНЬШУЮ ближайшую (2М, не 4М) — см. Е8.
- [ ] **З7.** Презентация `docs/к-встрече-2026-09-24.pptx` не проверена в PowerPoint визуально (проверил владелец —
      открылась). Шпаргалка `docs/к-встрече-2026-09-24.txt` отражает состояние на 24.09.

## Ж. Отложено по решению заказчика

- Распознавание плана чертежа (кнопки скрыты с 21.09) — приоритет понижен (§1.4).
- Межсерийные адаптеры 09623, 16723 (§1.2).
- Расценки работ по операциям, проценты работ/материалов — «на далёкое будущее» (§1.2).
- Битрикс — не обязателен (§1.6).

---

**Предлагаемый порядок (обновлён 26.09):** В1 → В2 → В3 → В9 → А5 → А6 → Б3–Б5 (поворот, undo, групповое
выделение) → остальное А и Б; А1 и Б8 — после ответа заказчика; Д — отдельный разговор с заказчиком.
