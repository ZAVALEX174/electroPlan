Тема: ElectroPlan — вопросы, без которых работа стоит

Добрый день!

Ниже собрано всё, что нам нужно от вас, чтобы двигаться дальше. Вопросы разбиты по
темам; отвечать можно прямо в этом файле под каждым пунктом.

Два вопроса (1 и 2) мы задавали в письме от 16 сентября — оно до вас, судя по всему,
не дошло, поэтому повторяем их здесь.


==============================================================================
1. КОД ФУНКЦИИ ИЗДЕЛИЯ В НОМЕНКЛАТУРЕ
==============================================================================

В программе готовый пост (собранный выключатель или блок) можно поставить в другое
помещение. Если в этом помещении выбрана другая серия, программа должна сама заменить
клавиши на такие же из этой серии: выключатель Neve Up заменить выключателем Arke.

Сейчас сделать это надёжно не получается: в номенклатуре нет признака, по которому
видно, что два изделия из разных серий выполняют одну и ту же функцию.

Пример. Выключатель 09001 (Neve Up, белый, 1 модуль). В серии Arke есть три белых
изделия на 1 модуль, и в колонке «Тип управления» у всех трёх стоит «выключатель»:
  - 19101.B — выключатель 1P осевой 16AX, 13,73 EUR;
  - 19105.B — переключатель 1P осевой 16AX, 16,67 EUR;
  - 19181.B — выключатель IR с реле 230 В, 141,81 EUR.
Программа не может понять, какой из них соответствует 09001. Ошибка стоила бы от 3 до
128 EUR на каждой клавише.

Колонка «Тип управления» заполнена только у 137 механизмов из 435. У остальных нет
вообще ничего, по чему можно было бы сравнивать.

1.1. Можно ли добавить в номенклатуру колонку «Код функции» — общий для всех серий код
     изделия? Например:
       «ВЫКЛ-1П-1М»   — выключатель 1P 1 модуль (09001, 19101.B, ...);
       «ПЕРЕКЛ-1П-1М» — переключатель 1P 1 модуль (09005, 19105.B, ...);
       «ИНВ-1П-1М»    — инвертор 1P 1 модуль (09013, 19113.B, ...);
       «ИК-ВЫКЛ»      — ИК-выключатель с реле (19181.B, ...).
     Сами коды могут быть любыми — важно, чтобы у одинаковых по функции изделий разных
     серий код совпадал, а у разных отличался.
     Если такой колонки не будет — можно ли хотя бы дать таблицу соответствия
     «артикул серии А -> артикул серии Б»?

     ОТВЕТ:

1.2. У 19105.B (переключатель) в колонке «Тип управления» стоит «В» (выключатель).
     У переключателя Neve Up 09005 стоит «П». Это опечатка? Если да, поправьте её,
     пожалуйста, в новом прайсе: от этой колонки зависит подбор механизмов для
     проходных выключателей.

     ОТВЕТ:

1.3. Если в серии помещения аналога клавиши нет — что делать? Мы предлагаем не ставить
     пост и сообщать, какой клавиши не хватает. Подходит?

     ОТВЕТ:

Пока ответа нет, программа ставит готовый пост только в помещение той же серии.
В помещение другой серии пост не ставится, и программа объясняет почему.


==============================================================================
2. ФОТОГРАФИИ НАКЛАДОК
==============================================================================

Фото мы берём с сайта vimar.ru. У 669 накладок из 1631 фото на сайте нет: там нет самих
этих артикулов, хотя они есть в номенклатуре и в прайсе. Ещё у 29 изделий вместо фото
стоит картинка-заглушка «нет фото». Всего 698 артикулов — список приложен отдельным
файлом.

Например, Plana «дерево, натуральный клён»: фото есть у рамок на 1, 2 и 7 модулей
(14641.61, 14642.61, 14657.61), но нет у рамок на 3 и 4 модуля (14653.61, 14654.61).
Больше всего таких у Arke — 453, у Neve Up — 38, у Plana — 35, остальные в Eikon.

Фото соседней рамки подставить нельзя: механизмы в программе рисуются прямо в монтажных
окнах накладки на фотографии, а у рамки другого размера другое число окон.

2.1. Можете ли вы прислать фотографии этих изделий — например, выгрузкой из вашей базы
     (в «Счёте с иллюстрациями» картинки у вас есть)? Удобнее всего файлы с именем по
     артикулу: 14653.61.jpg и т. п.

     ОТВЕТ:


==============================================================================
3. ПОДСВЕТКА КЛАВИШ — РАСХОЖДЕНИЕ ВАШЕГО ОТВЕТА С НОМЕНКЛАТУРОЙ
==============================================================================

В ответах вы написали, что подсветка 00936.250.Х идёт ко всем выключателям, КРОМЕ
коллекций NEVE (09ххх) и EIKON FLAT — «там вообще их единицы».

Но в номенклатуре колонка «Спрашивать подсветку» заполнена в том числе у 8 позиций NEVE
и у 31 позиции Eikon Flat — всего по данным подсветку принимают 106 позиций. На одной из
них (09021.N, NEVE) мы подсветку проверяли: она считается и попадает в предложение.

Это деньги: 9,60 EUR за светодиод.

3.1. Чему верить — вашему письму или номенклатуре? Если письму, мы перестанем предлагать
     подсветку для NEVE и Eikon Flat. Если номенклатуре — оставим как есть.

     ОТВЕТ:

3.2. Для схем со звонковыми кнопками вы просили уточнить напряжение подсветки: 220 В или
     00935.ХХ (12–24 В)?

     ОТВЕТ:


==============================================================================
4. АКСЕССУАРЫ К МЕХАНИЗМАМ
==============================================================================

Вы просили: при выборе механизма программа должна предлагать к нему аксессуар —
например, к терморегулятору термодатчик в пол, к выключателю подсветку. И дать
возможность отключить предложение или запомнить выбор один раз.

Мы готовы это сделать, но упираемся в данные.

4.1. Артикулов 02973, 02974 (регулятор) и 02965.1 (термодатчик в пол), которые вы
     назвали, в присланном прайсе нет. Пришлите их, пожалуйста, вместе с новым прайсом.

     ОТВЕТ:

4.2. Нужен список «к какому механизму какой аксессуар применим». Достаточно таблицы из
     двух колонок: артикул механизма -> артикул аксессуара. Или, если удобнее, колонка в
     номенклатуре. Как вам проще?

     ОТВЕТ:


==============================================================================
5. БЛАНК ДОКУМЕНТОВ
==============================================================================

В программе уже готово: логотип загружается и печатается в шапке коммерческого
предложения и листа монтажника; текст условий сделки вписывается свой и печатается
в конце предложения. Не хватает ваших материалов.

5.1. Логотип EPG — в векторе (SVG, AI, EPS) или крупным PNG.

     ОТВЕТ:

5.2. Текст «Соглашения сторон» (условия оплаты, поставка до 180 дней, претензии
     5 рабочих дней, срок действия цен) — пришлите, пожалуйста, отдельным текстовым
     файлом. Вы упоминали его в опроснике, но самого текста у нас нет.

     ОТВЕТ:

5.3. Подтвердите формат нумерации коммерческих предложений: EPG-2026-0001.

     ОТВЕТ:


==============================================================================
6. УРОВНИ ДОСТУПА
==============================================================================

Вы писали, что нужно «продумать, какое количество информации отображать для какого типа
пользователя», и что логотип в базовом тарифе — EPG, а в расширенном свой.

6.1. Назовите, пожалуйста, типы пользователей и что каждый из них видит и может. Хватит
     перечисления словами, например: «дизайнер — всё, кроме закупочных цен».

     ОТВЕТ:

6.2. В колонке «Группа доступа» номенклатуры встречаются коды 2 и 4. Что они означают?

     ОТВЕТ:


==============================================================================
7. НОВЫЙ ПРАЙС И КОЛЛЕКЦИЯ LINEA
==============================================================================

7.1. Вы говорили, что подготовите новый файл прайса — со статусами позиций (в наличии,
     под заказ, снята с производства) и с заменами для снятых. Когда его ждать? Пока
     статусов нет, программа не может предупредить, что изделие снято.

     ОТВЕТ:

7.2. Коллекция LINEA (30ххх, 31ххх) — вы говорили, что у неё другой принцип формирования
     механизмов и своя подсветка, и что номенклатуру подготовите отдельно. Когда?

     ОТВЕТ:


==============================================================================

Приложение: список артикулов без фотографий (698 позиций), отдельным файлом.

С уважением,
