{
  "name": "electroplan",
  "version": "0.1.0",
  "private": true,
  "description": "ElectroPlan Pro — веб-планировщик электрики. Прототип без сборщика (см. PLAN.md 2.2): рантайм-зависимостей нет. npm нужен для автотестов и тулинга (пересборка каталога из прайса VIMAR).",
  "type": "commonjs",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "build:catalog": "node tools/build-catalog.mjs",
    "build:attrs": "node tools/build-catalog-attrs.mjs"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
