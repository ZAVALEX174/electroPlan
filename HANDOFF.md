# HANDOFF — ElectroPlan Pro

> **Это входной документ проекта.** В начале каждой новой сессии Claude обязан прочитать
> этот файл первым, чтобы восстановить контекст (см. правило в `../CLAUDE.md`).
> В конце каждой задачи в раздел **«Журнал работ»** добавляется запись: что сделано, как
> сделано, с какими проблемами столкнулись.
>
> Обновлён: 2026-08-05 (деплой превью тимлиду/заказчику, каталог на реальной
> номенклатуре, фикс раскладки постов, взрыв-схема в листе монтажника с фото,
> открытый вопрос к заказчику по суппорту NEVE UP, встреча с заказчиком 06.08).

---

## 1. Что это за проект

**ElectroPlan Pro** — веб-планировщик электрики (проектирование ЭУИ — электроустановочных
изделий: розеток, выключателей, постов). Пользователь загружает план помещения, расставляет
на нём электрику, привязывает элементы к комнатам, собирает электрические посты из модульных
рамок и механизмов и на выходе получает смету и коммерческое предложение (PDF).

- **Заказчик** — компания **ЦентрСвет** (отдельный клиент; лежит внутри `РАБОТА ЛЮКСАР/myProgects`,
  но к ЛЮКСАР отношения не имеет).
- **Поставщик оборудования для каталога** — **VIMAR** (не заказчик). Каталог наполняется из
  прайса `Прайс VIMAR Евро 01.07.26 (2) (2).xls` (валюта EUR, дата 01.07.2026).
- **Расположение кода:** `electro/electroplan-project/`.
- **Git:** реальный репозиторий — `electroplan-project/.git` (один коммит `first commit`, дальше —
  много незакоммиченного). `.git` в корне `electro/` — пустой/нефункциональный, **не трогать**.

## 2. Для чего он нужен

Инструмент проектировщика: ускорить путь «план помещения → расстановка электрики → смета/КП».
Целевой сценарий и однопользовательский/многопользовательский режим ещё окончательно не
зафиксированы заказчиком (см. `PLAN.md`, раздел 0). В перспективе — интеграция в **1С-Битрикс**.

## 3. Что используется / технологии

**Текущий прототип — полностью клиентский, на ванильном JS, без сборки.** Запускается статикой.

| Слой | Технология |
|------|-----------|
| Фронтенд | Ванильный JavaScript (ES-модули не используются, скрипты через `<script>`), HTML, CSS |
| Разметка приложения | `index.html` + `css/styles.css` |
| Логика редактора | `js/app.js` (~63 КБ — монолит: план, стены, комнаты, посты, смета, PDF) |
| Слой данных | `js/data.js` → `window.DataService` (режимы `mock` / `api`); каталог — `js/catalog-vimar.js` |
| Хранение (прототип) | LocalStorage (шаблоны постов, проекты); изображения — data-URL |
| Импорт планов | `js/planImport.js` — PNG/SVG напрямую; PDF через **PDF.js**; DXF — свой парсер (`vendor/dxf-parser`); DWG→DXF через **wasm-конвертер** (`vendor/dwgdxf`) |
| Классическая сегментация комнат | `js/roomSegmentation.js` на **OpenCV.js** (`vendor/opencv.js`, ~10 МБ): Otsu → сеть стен → distance transform + watershed → слияние → ортогонализация контуров |
| ML-распознавание | `js/floorplanML.js` — **YOLOv8** через **onnxruntime-web** (WebGPU→WASM fallback, `vendor/ort/`); модель `vendor/models/floorplan-yolo.onnx` (103 МБ FP32, 9 классов) |
| PDF (КП) | Печатная страница браузера + системный диалог «Сохранить как PDF» |
| Локальный запуск | `python -m http.server --directory electroplan-project 8420` (см. `.claude/launch.json`, конфиг `electroplan`) |

**Целевой бэкенд — 1С-Битрикс** (архитектура выбрана, скелет создан, но не подключён):
- Модуль `centrsvet.electroplan`, PHP-namespace `Centrsvet\Electroplan`, компонент `centrsvet:electroplan.app`.
- Скелет в `local/` (modules + components): репозитории товаров/постов/проектов, AJAX-контроллеры.
- Каталог — из инфоблока «Торговый каталог»; посты/проекты — Highload-блоки; авторизация — штатная.
- Принцип: фронт ходит за данными **только через `window.DataService`**; под Битрикс он реализуется
  адаптером поверх `BX.ajax.runAction`, а `app.js` не меняется.
- Для сервера подготовлены (как ориентир): `database/schema.sql` (PostgreSQL), `api/README.md`.

## 4. Ключевые документы проекта

- `PLAN.md` — главный план-требования (разделы 0–7), редактируется заказчиком; источник истины по задачам.
- `PLAN-segmentation.md` — техжурнал автораспознавания комнат (Шаг 1 CV + Шаг 2 ML).
- `INTERFACE.md` — карта интерфейса.
- `README.md` — краткое описание прототипа и как подключить БД.
- `docs/WORK_REPORT_2026-07-23.md` — подробный отчёт по итогам работ до 23.07.2026.
- `../floorplan-recognition-bitrix-architecture.md` — полное архитектурное видение распознавания планов.

## 5. Что уже сделано

**Редактор и проектирование:**
- Загрузка плана, ручная обрисовка стен, подписи комнат, компактные пиктограммы объектов,
  карточка при наведении, перемещение объектов.
- Конструктор электрических постов: рамка из реального каталога, модульное заполнение проёмов
  с учётом `moduleSpan`, индикатор занятых/свободных модулей, обязательный подрозетник, фильтрация
  списка по остатку места, сохранение шаблона поста и размещение на плане.
- Автопривязка оборудования к комнате (point-in-polygon, grid — фолбэк).
- Смета (оборудование / материалы / работы) и печатная форма КП с сохранением в PDF.

**Импорт планов (`planImport.js`) — законченный клиентский сценарий:**
- PNG/SVG напрямую; PDF через PDF.js (выбор страницы в многостраничном, растеризация, лимит пикселей);
  ASCII DXF (model space: линии/полилинии/дуги/сплайны/блоки); DWG→DXF (R13–R2018) через wasm.
- Ограничения: файл ≤100 МБ, лимит сложности, понятные ошибки для бинарного DXF/повреждённых файлов.

**Каталог VIMAR (`catalog-vimar.js`, ~443 КБ):**
- Разобран прайс (7 376 строк), подготовлена импортная книга и SQL-схема.
- Рабочий каталог для приложения: 399 товаров (340 механизмов, 58 рамок, 1 подрозетник) с реальными
  артикулами, ценами EUR, изображениями, сериями совместимости (Idea, Neve Up, часть KNX).
- Для всех 340 механизмов проставлен реальный `moduleSpan` (по официальным каталогам Vimar).

**Автораспознавание комнат:**
- *Классический CV (`roomSegmentation.js`)* — работает на чётких планах; кнопка «Определить комнаты».
  Ортогонализация контуров, привязка к линиям стен, ROI по контуру здания (отсечение рамки/штампа).
- *Ручная правка контуров* — инструмент «Правка комнат»: тянуть вершины, добавлять и удалять их;
  правки переживают повторное авто-определение.
- *Масштаб и площадь* — кнопка «Задать масштаб» (эталонный отрезок → px/м), площадь комнат в м²
  считается по контуру; ручной ввод площади остаётся приоритетным.
- *ML-детекция (`floorplanML.js`)* — **фича «Распознать элементы (ML)» готова и проверена**: YOLOv8
  в браузере выводит цветные боксы стен/дверей/окон/колонн/лестниц + легенду со счётчиками.
  Валидировано на образце `639`: ~25 элементов точно по месту.

**Битрикс:** создан скелет модуля/компонента/репозиториев/контроллеров в `local/`.
`ProductRepository.php` расширен полями изображений, совместимости и `mountRect` (под текущий фронт).
**Статус на 2026-08-05: скелет по-прежнему заглушка** — `ProductRepository::getProducts()`
возвращает `[]` с `// TODO (PLAN 6.2)`, `DataService.mode = "mock"`. Проект целиком на этапе
HTML/CSS/JS-прототипа, к Битриксу не подключён (см. `CLAUDE.md`, «Текущий этап работы»).

**С 2026-07-27 по 2026-08-05 (кратко — подробности в журнале ниже и в `git log`):**
- Каталог **перестроен на реальную номенклатуру заказчика** (файл «Номенклатура новая.xls»,
  2148 позиций) вместо эвристического разбора прайса — серия, монтажный стандарт, тип стены
  берутся из номенклатуры напрямую, не угадываются.
- Автоподбор суппорта/коробки под накладку и тип стены; правило «одна накладка — одна коробка»;
  IP55-корпуса и подсветки корректно выведены из состава поста (не механизмы).
- **Раскладка постов почищена от бага**: многомодульный механизм больше не может встать верхом
  на импост (границу между постами) — вместо жадного алгоритма теперь полный перебор с возвратом,
  раскладка находится, если она вообще существует.
- **Лист монтажника получил взрыв-схему** поста: накладка / механизмы (стеком) / суппорт / коробка
  разнесены, у каждой детали артикул и, где есть в каталоге, настоящее фото товара (не только
  накладки — фото теперь у любой детали через `EPCatalog.productImage()`). Дополняет прежнюю
  картинку собранного поста, не заменяет.
- **Деплой настроен** — см. раздел 8 ниже, это НОВОЕ с 2026-08-05, в прежних версиях HANDOFF
  отсутствовало.
- `PLAN.md` актуализирован по факту кода 2026-08-05 (не полагайтесь на дату «на 2026-07-24»
  в старых записях этого файла — читайте `PLAN.md` напрямую, это живой источник статусов).

## 6. Что предстоит сделать

**Статусы по каждому пункту — в `PLAN.md`, это живой документ, актуализирован 2026-08-05.**
Не дублирую его здесь целиком, ниже — только то, что реально блокирует или горит прямо сейчас.

**Открытый вопрос к заказчику (блокирует часть каталога):**
- Суппорт для центральных накладок NEVE UP (`09661.*`/`09671.*`, 24 позиции) — см.
  `docs/записка-заказчику-подбор.txt`, повторный вопрос отправлен 26.08
  (`docs/письмо-заказчику-вопросы-2026-08-26.txt`, п. 1). Часть закрылась сама, по номенклатуре:
  `09661.*` монтируется с `09602.1`/`09603.1` (тип задаёт коробка), `09672.*` — с `09606`.
  Открытым остался только `09671.*` — там ремарка называет типоразмер («коробка и супорт на
  3 модуля»), но не артикул. Пост считается с нашим кандидатом `09613`, и во ВСЕХ документах
  он помечен «(предположительно)» — смета больше не занижена, но и догадка за факт не выдаётся.
  Тем же флагом (`postComposition.supportAssumed`) помечены `22673.1.*`/`22683.1.*` → `21613`
  и `09679.*` → `09613`; всего 36 накладок из 1631. Правило проекта (`process-work-by-blocks`
  в памяти): новый блок работы по каталогу не начинать, пока не будет ответа.

**Встреча с заказчиком 2026-08-06:**
- Подготовлен полный список вопросов — `docs/вопросы-заказчику-встреча-2026-08-06.txt` (не в git,
  как и другие письма/записки заказчику в `docs/`). Приоритет на встрече: суппорт NEVE UP (выше)
  и раздел «сценарий использования» (кто пользователь, локальный инструмент или веб-сервис) —
  от него зависит весь дальнейший объём работы, раздел 0 `PLAN.md` до сих пор не закрыт.

**Рыночное исследование аналогов** (по запросу владельца, 2026-08-05) лежит ТОЛЬКО в scratchpad
сессии, **не в репозитории** — файл `market-research-electroplan.md`, отправлен владельцу как
файл в чате. Если понадобится в следующей сессии — попросите переслать или пересоберите (задача
была: найти аналоги ElectroPlan среди конфигураторов VIMAR/Legrand/Gira и др., вывод — прямого
аналога на рынке не нашлось).

**Не закрытые вопросы раздела 0 `PLAN.md`** (нужны ответы заказчика, из общего списка вопросов
выше): целевой сценарий (0.2), источник каталога из Битрикс (0.4/0.5), этап интеграции (0.7).

---

## 7. Деплой и ветки (настроено 2026-08-05, раньше не было)

Репозиторий приватный на GitHub (`ZAVALEX174/electroPlan`, владелец — свой аккаунт), хостинг
превью — **Cloudflare Pages** (бесплатно, деплоится сам при пуше в подключённые ветки). Ни
заказчик, ни тимлид **не коллабораторы репозитория** — у них только ссылки на превью, кода не
видят.

- **Ветка `main` → тимлид.** Ссылка: **https://electroplan-573.pages.dev**. Обновляется сама при
  каждом `git push origin main`. Это реальный текущий код — пушьте в неё как обычно.
- **Ветка `client` → заказчик.** Ссылка: **https://client.electroplan-573.pages.dev**. НЕ живёт
  как обычная ветка — это **урезанный снимок конкретного коммита из истории `main`**, вручную
  выбираемый и пересобираемый (владелец решает, до какого момента заказчик может видеть прогресс).
  Сейчас (2026-08-05) указывает на коммит `a66122e` (01.08, «Собранный пост из настоящих
  фотографий») — то есть заказчик НЕ видит ничего из работы после этой точки (каталог на
  реальной номенклатуре, фикс импоста, взрыв-схема — это уже после `a66122e`).
  - Если нужно подтянуть `client` до другого коммита — процедура: `git checkout -B client <SHA>` →
    `git rm -r --cached .` → `git checkout -- .gitignore` → `git add index.html css js` (в ветке
    остаётся ТОЛЬКО сайт, никаких внутренних доков/`.claude/`/`local/`) → закоммитить → `git push
    origin client --force-with-lease` (это осознанный force — история `client` не линейна
    относительно `main`, так и задумано) → вернуться на `main` (`git checkout main`; если ругается
    на untracked-конфликты типа `.idea/`, `chertez/`, `vendor/` — это старый `.gitignore` снимка не
    исключает их, убрать через `git stash push -u -- <пути>`, не удалять руками).
  - После такой пересборки в `git stash` накопилось несколько записей (`leftover ... snapshot
    files`) — они безопасны и не нужны, можно дропнуть (`git stash clear` или по одной `git stash
    drop`), просто раньше не давали дропнуть автоматические ограничения сессии.
- Если попросят описание того, что видно в конкретном коммите (для письма/презентации заказчику)
  — смотреть `git log --oneline <SHA>` и переводить на человеческий язык фичи, а не пересказывать
  commit-сообщения дословно (в них внутренний жаргон). Пример готового текста для `a66122e` —
  в истории чата этой сессии, не сохранён отдельным файлом.

---

## 7.1. Правило разработки: отдельный состязательный проход

> Выведено из практики 27–28.08.2026, когда почти каждый раунд ревью находил дефект
> **в том, что предыдущий раунд только что починил**.

**Правку проверяет не только тот, кто её сделал, и не только на вопрос «работает ли».**
Нужен отдельный проход, который ищет **края правки** — места, где она должна была
примениться, но не применилась.

Причина не в качестве исполнителя. Правка почти всегда имеет края, которые видно только
со стороны: тот, кто чинит, смотрит на дефект, а не на все места, где живёт то же правило.
Состязательный проход задаёт другой вопрос — не «работает ли», а **«где ещё это должно
было примениться»**.

**Как это выглядело на практике:**
- порядок модулей в листе монтажника применили к двум блокам карточки **из трёх**;
- защиту от осиротевшей группы света сделали только «вперёд» — старые сохранённые проекты
  остались с фантомными местами, которые оживают после перезаливки прайса;
- подтверждение опасной команды получило верхнюю границу окна, но **не нижнюю** —
  и снималось обычным двойным кликом;
- тип стены отвязали от проекта у размещённого поста, а **соседний селектор в том же окне**
  (схема электрики) остался с тем же дефектом;
- склеиваемый ключ починили в кэше подбора, но **не в ключе сортировки** — и результат
  снова зависел от порядка входа (36.94 € против 48.32 €).

**Что это значит для работы над проектом:**
1. После правки спрашивать не «починено ли», а «сколько ещё мест живут по тому же правилу
   и все ли они поправлены».
2. Правило выносить в одно место (чистую функцию), а не размножать по потребителям —
   тогда у правки физически не будет краёв. Именно так сделаны `EPPosts.boxCount`,
   `EPInstallSheet.cardModuleOrder`, `EPConfirmRepeat.press`, `EPEstimate.postPrice`.
3. Состязательному проходу ставить задачу словами «ищи дефект, не подтверждай»
   и требовать доказательства: вход, ожидание, факт. Отчёт «всё хорошо» без доказательств
   не считается проверкой.
4. Особое внимание — **обратной совместимости правки**: она чинит новые данные, но что
   с уже сохранёнными проектами? Это самый частый пропущенный край.

**Отдельно про тесты.** Зелёный тест не значит проверенное правило. Дважды за сессию
тест был негоден: один проверял, что рендерер выводит переданную ему строку (тавтология),
другой разбирал состав по запятой, а 45% реальных имён каталога сами содержат запятую —
на фикстуре зелено, на настоящих данных мусор. Поэтому мутационная проверка (сломай код —
покраснел ли тест) не формальность, а единственный способ узнать, что тест что-то держит.

## 7.2. Как организована работа: сабагенты и роль Claude

> Сложилось за 26–28.08.2026, когда за одну сессию через сабагентов прошло около 40 задач.

### Роль Claude — оркестратор, а не кодер

**Claude сам код не пишет.** Он ставит задачи сабагентам, проверяет результат
СВОИМИ РУКАМИ (не по отчёту), принимает решения и выносит на утверждение владельцу.
Это правило проекта, и оно себя оправдало: агент, погружённый в один файл, видит его лучше,
а оркестратор видит связи между задачами и ловит то, что агент считает «не своей зоной».

**Что Claude делает лично, не делегируя:**
- читает исходные данные заказчика (номенклатуру, прайс, письма) и устанавливает ФАКТЫ,
  прежде чем ставить задачу — иначе агент потратит полчаса на то, что проверяется одним запросом;
- прогоняет `npm test` и `git status` сам после каждого возврата агента;
- читает дифф ключевых мест, а не только отчёт;
- решает продуктовые развилки или выносит их владельцу;
- коммитит и пушит (агентам это ЗАПРЕЩЕНО — см. ниже).

**Важный урок:** дважды за сессию Claude дал агенту неверное указание, потому что поверил
чужому выводу вместо исходных данных (откат приоритета суппортов; гипотезы о причинах B4/B5 —
обе оказались неверны). Ставя задачу, лучше приложить проверенные факты с числами,
чем формулировать гипотезу — агент будет её отрабатывать вместо поиска настоящей причины.

### Структура задачи: разведка → реализация → двойная проверка

Стандартный workflow проекта:

1. **Разведка** (read-only). Отдельный агент читает код и отчитывается: где что лежит,
   какие точки встраивания, какие риски. Кода не трогает. Это окупается: разведка по B4/B5
   опровергла обе гипотезы и нашла настоящие причины, разведка по конструктору нашла, что
   «Функциональная группа» не доходит до рантайма.
2. **Реализация.** Получает отчёт разведки как карту, но обязана перепроверять.
3. **Две проверки параллельно, с РАЗНЫМИ заданиями:**
   - *независимый контроль* — «проверь фактами, отчёту не доверяя»: сам прогони тесты,
     сам замерь, сам открой в браузере;
   - *состязательная проверка* — «ищи дефект, не подтверждай; не чини — доказывай».
   Разные вопросы дают разный улов. Независимый ловит несоответствие заявленному,
   состязательный — края правки (см. §7.1).

Сложные блоки идут в несколько раундов: доводка после ревью, снова проверка. Модуль групп
света потребовал шести раундов, и каждый находил денежный дефект.

### Как ставить задачу агенту

Что обязательно должно быть в промпте:

- **Проверенные факты с числами.** «Реле 03992 в каталоге отсутствует, 0 совпадений;
  найденные "реле" — это IR-выключатели 09181/19181/20181» вместо «проверь, есть ли реле».
- **Цитата заказчика**, если задача из его слов. Агент иначе не поймёт, что именно нужно.
- **Что НЕЛЬЗЯ сломать** — явным списком. Фильтр по остатку места, раскладка с импостами,
  превью, суппорты — всё это стоило труда и легко ломается мимоходом.
- **Границы зоны при параллельной работе:** какие файлы НЕ трогать, потому что их правит
  другой агент. Без этого они затирают друг друга.
- **Запрет коммитить.** Агенты не коммитят и не пушат — иначе история станет мусорной,
  а откатывать будет нечего.
- **Требование доказательств:** «приведи фактические числа», «приведи в browserCheck,
  что увидел». Отчёт «всё хорошо» без доказательств не принимается.
- **Мутационная проверка** для всего, что считает деньги: сломай код — покраснел ли тест.
- **Структурированный вывод** (schema) — тогда результат можно разобрать программно,
  а не вычитывать простыню.

### Параллельная работа

Больше двух линий одновременно запускать не стоит: почти всё в проекте сходится в `app.js`
и `index.html`. Хорошо параллелятся задачи, где одна делает новый чистый модуль, а вторая
правит другую подсистему. Обеим прописывается, чьи файлы не трогать и что в `index.html`
менять только свои строки.

Отдельная грабля: агенты, проверяющие в браузере, **делят localStorage**, если открывают
один и тот же адрес. Двум линиям нужно разводить порт или origin (`localhost` против
`127.0.0.1`), иначе они затирают проект друг друга и замеры врут.

### Размер задачи и частота коммитов

> Выведено 28.08.2026, после того как сабагент умер посреди раунда правки.

**Задачи агентам держать короткими, каждую готовую часть коммитить сразу.**

Причина техническая: расход по пятичасовому лимиту Claude **не видит в реальном
времени** — ни процента, ни приближения к границе. Узнать можно только постфактум,
когда агент падает с `You've hit your session limit`. Так и случилось: раунд 2 по
черновику комнаты оборвался на середине, и уцелел он лишь потому, что агент успел
записать файлы до обрыва. Отчёта не было — состояние пришлось восстанавливать
чтением дерева.

Что из этого следует:
1. Крупный блок дробить на **самоценные части**, каждую из которых не жалко
   закоммитить отдельно. Пример: «перенос пользовательских полей при пересчёте
   контуров» отдельно от «поля схемы у комнаты» и от «по-комнатного расчёта» —
   первая часть полезна сама по себе, она чинит потерю имён комнат.
2. После возврата агента — сразу проверить своими руками и закоммитить, **не копить**
   несколько незакоммиченных правок в дереве. Иначе один обрыв ставит под удар всё.
3. Ставить задачу так, чтобы **даже частичный результат был полезен**.
4. Про остаток лимита говорить честно: не видно. Посмотреть может владелец — `/usage`.

### Чего от агентов ждать не надо

- Что они сами заметят соседний край правки — для этого есть состязательный проход.
- Что «тесты зелёные» означает «правило проверено» — см. §7.1 про негодные тесты.
- Что они правильно поймут продуктовую развилку — такие решения принимает Claude
  или владелец, агенту их надо давать готовыми.


## 🚦 КЛЮЧЕВОЕ СЛОВО «ПЛАН-0» — НАЧАТЬ РАБОТУ ПО ПЛАНУ НЕМЕДЛЕННО

> **Владелец, 02.09.2026:** если он присылает `ПЛАН-0` — это значит «выполняй план ниже, начиная
> с Этапа 0, по порядку». Ничего не переспрашивать, не пересматривать приоритеты, не предлагать
> альтернатив. План согласован, реализация должна быть чёткой и начаться сразу.
>
> Два пункта (3.3 и 3.4) требуют решения владельца — дойдя до них, задать вопрос и продолжать
> остальное, а не останавливать весь план.

# ПЛАН РАБОТ, согласован 02.09.2026

Порядок построен на двух принципах: **сначала то, без чего остальное непроверяемо**, и
**группировка по общей причине, а не по симптому** — половина пунктов имеет один корень, и чинить
их порознь значит снова оставлять края (§7.1).

## Этап 0 — расчистка. Без неё всё остальное громоздится на непроверенном

**0.1. ✅ СДЕЛАНО 02.09 (main = 666efe9, 787 тестов, ветка влита и удалена). Было: повторная двойная проверка `6e096c3` и слияние ветки `wip/frame-modules-from-catalog` в `main`.**
Сейчас вся работа висит на невлитой ветке. Прошлый раз именно повторная проверка починок нашла два
новых блокера — шаг не пропускать. Ветка на актуальном `main`, 785 тестов.

**0.2. ✅ СДЕЛАНО 02.09.** Общий стенд `vm` + DOM-шим в `tests/helpers/`.**
Решение владельца 02.09: сначала тестируемость, не разбиение `app.js`. Стенд уже собирался минимум
ПЯТЬ раз (в тестах репозитория и трижды заново в scratchpad проверяющими агентами) и каждый раз
выбрасывался. Исполнитель обязан найти все копии САМ, перевести на общий хелпер ВСЕ без исключения,
сохранить свойства, на которых держались находки (шим `<select>` соблюдает спеку: присвоение
`.value` отсутствующей опции снимает выбор), и доказать таблицей «мутация → красных до → красных
после», что ни один тест не ослаб. Продакшн-код не трогать.

> **Закрыто 02.09.** Стенд вынесен в `tests/helpers/appStand.js` (189 строк), на него переведены шесть тестов: минус 228 строк дублирования при плюс 60.
> **Полнота перевода проверена мной:** ни одного теста со своим `vm` и ни одного рукодельного DOM-шима вне стенда не осталось.
> **Таблица мутаций сведена мной, 12 из 12 краснеют:** инверсия isOutsideRooms 17; отключение предупреждения 1; frameMissing=false 2; снятие renderProperties 1; ЗАКОММЕНТИРОВАТЬ syncNoRoomClass 1; hasExplicitFrame→"" 1; builderCapacity ||1 1; frameSlotOptions без extra 1; снятие очистки builderCapacity 2; ненулевой remaining 1; снятие renderBuilderCatalog 2; обрыв доставки баннера 2. Покрытие не ослабло, по критерию метки усилилось (17 против 12).
> **Мелочь на будущее:** у выражения `hasExplicitFrame?explicitFrameId:frameSelect.value` ДВА вхождения, и порча одного оставляет прогон зелёным — второе держит поведение. Не регресс, было и до перевода.

## Этап 1 — деньги

**1.1. ✅ СДЕЛАНО 02.09 (4456bd0, 790 тестов).** Накладка с `active:false` не подменяется молча.**
Механизм дефекта установлен по коду: `byKind("frame")` фильтрует по `active`, а `product(id)` — нет.
На первом рендере накладка приходит явно через `dataset.preferredFrameId` и попадает в список
отдельной веткой; строка `delete frameSelect.dataset.preferredFrameId` снимает признак, и на втором
рендере `frameList.some(...)` даёт `false`, а `selectedFrameId` молча падает на `frameList[0]` —
первую накладку каталога. Замер: 27,48 EUR превращаются в 9,35 EUR. Новая защита `frameMissing` не
срабатывает, потому что товар находится.
**Решение владельца:** накладку ОСТАВИТЬ выбранной на любом рендере, пометить «снята с
производства», сохранение РАЗРЕШИТЬ. Проект мог быть сделан раньше, чем позицию сняли, изделие
физически существует. Дёшево: механизм уже написан рядом для пропавшего артикула.

**1.1a. ✅ СДЕЛАНО 02.09 (4525bc0, 795 тестов).** Дефект оказался серьёзнее: снятый механизм выбрасывался фитом и исчезал из state.builder.slots НАВСЕГДА, то есть из сметы и всех документов; и мест было ДВА — фит после рендера и фит при выборе карточки (ронял СОСЕДНИЙ снятый). Замер: 16,02 EUR и с активным, и со снятым. Было: найдено при 1.1 — близнец того же дефекта у МЕХАНИЗМОВ, денежный.**
Механизмы фильтруются тем же `byKind` (с `active`), и `EPBuilderSlots.allowedTokens` выкидывает токен слота, чьего товара нет в списке. Значит **уже стоящий в посту снятый с производства механизм молча удаляется** при перерисовке вместе со своей стоимостью. То же правило, что в 1.1, другая сущность. Чинить тем же решением: удержать выбранное, пометить, новым не предлагать.
Отдельно ниже по важности: суппорты и коробки тоже подбираются из `byKind`, но они не хранятся в посту, а выводятся — там риск иной (снятый кандидат просто выпадёт из пула подбора).

> **Флажок владельцу по 1.1:** правка попутно задела **18 многорядных накладок 14/21**: раньше их тоже молча подменяло на чужую, теперь удерживает. По сути улучшение, но формально — изменение поведения этих позиций; пересекается с пунктом 3.2.

**1.1b. ТРЕБУЕТ РЕШЕНИЯ ВЛАДЕЛЬЦА — третья форма того же состояния.** Механизм, у которого АРТИКУЛ ПРОПАЛ из каталога (а не помечен неактивным), сейчас отсеивается фитом — посчитать по нему нечего. У накладок для этого случая своё поведение (красный баннер + блокировка сохранения). Распространять ли его на механизмы — решает владелец.

**1.2. Шаг A — привязка объекта у границы контура.**
Тот самый «1 px = 11 EUR»: выключатель у дверного проёма стоит центром ровно на контуре и выпадает
из комнаты. Допуск вырезан как неверный по замыслу. Дискриминатор выбран — «есть ли стена между
точкой и контуром»; прототип замерен, подводный камень с `segmentsIntersection` записан в записи от
31.08 (вечер). Самая крупная задача плана.

## Этап 2 — документы перестают врать молчанием

**2.1. Одно правило «накладка недоступна или снята» на ВСЕХ потребителей.**
Смета и свод поставщику честно пишут «Накладка не найдена», а лист монтажника, взрыв-схема,
подсказка на плане и «Раскладка постов» в КП не упоминают её вообще — прямое нарушение правила
проекта «два документа об одном проекте не могут противоречить». Сюда же: кнопка «Лист монтажника»
в окне конструктора не заблокирована, окно только что запретило сохранять — а документ выдаёт.
**Одной задачей**, потому что край один: чинить по одному потребителю значит гарантированно забыть
следующего.

## Этап 3 — интерфейс

**3.1. Toast «Объект размещён вне комнаты»** шлётся на каждый объект, пока комнат в проекте нет —
ровно тот шум, который правило метки объявило недопустимым. Свести к единому критерию
`EPRoomAssign.isOutsideRooms`. Дёшево, критерий уже есть.

**3.2. Тупик на многорядных 14/21 и недостижимость 18 многорядных накладок** — общий корень:
`frameSlotCount` не разбирает многорядные и отдаёт `null`. Следствия: пост на такой накладке
показывает «рамка заполнена», а «Сохранить» мертво навсегда без объяснения; и сами 18 накладок не
появляются в списке ни при одном значении селектора, то есть их нельзя выбрать в принципе. Одна
задача.

**3.3. Строка «⚠ Вне помещений» прячется вместе с правой панелью** при ширине ≤1100 px
(`css/styles.css`, `@media(max-width:1100px){.right-panel{display:none}}`), а оранжевые кольца на
плане остаются. Замер мой: при 1090 px панель `display:none`, строка в DOM есть, высота 0.
**РЕШЕНИЕ ВЛАДЕЛЬЦА 02.09: перенести строку в СТРОКУ СТАТУСА под планом** — туда, что не скрывается. Дублировать НЕЛЬЗЯ: вторая копия правила — ровно тот дефект, что чиним весь день (§7.1).
Задача была составлена, но НЕ ЗАПУЩЕНА — кончился лимит. Главное в ней: строка уходит из `#lightingSummary` СОВСЕМ (одно место, не два); критерий `EPRoomAssign.isOutsideRooms` не трогать и четвёртого потребителя не заводить; в статус уже пишут другие — разобраться, чтобы не затирать ни в одну сторону; медиазапрос `.right-panel` не трогать; тесты переориентировать, но не ослабить (таблица «мутация → красных до → после»).

**3.4. Черновик комнаты — ПУНКТ ИСПРАВЛЕН 02.09, прежняя формулировка была неверна.**
Я записал «черновик теряется чаще» не проверив код. Проверка: `renderProperties` начинается
строкой `flushRoomDraft()` (`js/app.js:1270`), а она нормализует имя и площадь **ровно теми же
правилами, что кнопка «Сохранить изменения»**, пишет их в комнату и зовёт `persistProject()`.
`EPRoomDraft.commit` отказывает только в двух случаях: комнаты уже нет в проекте (правильно — не
воскрешаем) и холостой ход (ничего не менялось). Валидации формата площади в проекте нет.

**Значит черновик не теряется, а наоборот — коммитится.** Но следствие всё равно есть, и оно
требует решения владельца: пять путей, начавших звать `renderProperties`, теперь **молча
сохраняют** имя и площадь комнаты при рисовании стен, линий разметки и перетаскивании подписи.
То есть развилка от 28.08 («сохранять по вводу или беречь черновик») **де-факто уже решена в
коде в пользу «сохранять»**, просто это никто не выбирал сознательно.

**ТРЕБУЕТ РЕШЕНИЯ ВЛАДЕЛЬЦА:** подтвердить, что автосохранение полей комнаты — желаемое
поведение (тогда кнопка «Сохранить изменения» вводит в заблуждение и её надо убрать или
переименовать), либо вернуть черновик до кнопки (тогда `flushRoomDraft` из `renderProperties`
надо убирать, а это отдельная правка с своими краями).

## Вне очереди плана — не код

Заказчик не видел работы с 01.08 (ветка `client` указывает на старый снимок). Четыре блокирующих
вопроса по каталогу: суппорт `09671.*`, суппорт `22673.2.*`, «звонковые кнопки», импульсное реле.
По правилу проекта новый блок работы по каталогу не начинать без ответов.


---

## 8. Журнал работ

> ### ПРАВИЛО ПРОЕКТА: живых проектов нет, миграция сохранённых данных — НЕ блокер
>
> **Владелец, 30.08 (повторно):** «у нас нет рабочих проектов, мы только разрабатываем эту
> программу; максимум, что скидывают, — это примеры».
>
> Отсюда: изменение, которое меняет расчёт уже сохранённых проектов при открытии, **не
> является блокирующим** и не требует ни миграции, ни предупреждения, ни флага версии.
> Не выносить это на решение владельца повторно — вопрос закрыт для всего этапа разработки.
>
> **Когда правило перестанет действовать:** появятся проекты заказчика или уйдут сметы
> клиенту. Тогда — и только тогда — вернуться к вопросу молчаливого пересчёта.
>
> ⚠️ Claude уже спрашивал об этом дважды (по разделению групп света и по допуску привязки),
> потому что записал ответ как условие конкретной правки, а не как правило этапа. Не повторять.


> **Добавляй новую запись сверху. Формат: дата · что сделано · как сделано · проблемы.**

### 2026-09-02 (ПЛАН-0, заход 1) — Пункт 0.1 ЗАКРЫТ, ветка конструктора влита в main

**`main` = `666efe9`, 787 тестов, запушено. Веток осталось две: `main` и `client`.**

## ✅ 0.1 сделан целиком

Повторная двойная проверка `6e096c3` пройдена, найденное закрыто, ветка влита squash-ом и
**удалена сразу после слияния** (локально и на сервере) — по правилу, которое раньше я же нарушал.

**Что подтвердила двойная проверка (числа их, прогоны их):**
- нормальный путь `renderBuilder` не изменился **ни на один вызов из 18 624 сравнённых сценариев**
  (164 накладки × наборы механизмов × варианты селектора, сравнивалась полная трасса побочных
  эффектов, а не числа на глаз); контрольная порча ловится в 308 сценариях — стенд не слепой;
- деньги не тронуты, денежные модули вне диффа;
- состояние «накладка недоступна» доведено: одна ошибка вместо двух, полоса и превью пусты,
  каталог не обещает свободных модулей, механизмы 5 из 5;
- побочный эффект подтверждён замером: смена селектора модулей больше не режет механизмы
  (5-5-5-5 против 5→2→2→2 до правки).

**Что нашли и я закрыл до слияния (коммит `d7785e5`):**
1. **У правила «накладка непригодна» не было нижней границы.** `!!requestedFrameId` отсекал пустую
   строку, хотя комментарий строкой выше прямо называл это состояние явным «накладки нет». Пост с
   пустым `frameId` молча получал первую накладку каталога, «Сохранить» было активно, и
   `savePostBuilder` записал бы её id. Теперь конструктор различает ТРИ состояния так же, как их
   различает смета (`js/estimate.js:193-197`): найдена / артикул задан, но пропал / накладки нет
   вовсе. У последних двух РАЗНЫЕ формулировки — это разные причины.
2. **Тест держал одно обещание коммита из четырёх.** Три мутации оставляли 785 зелёными: снятие
   очистки полосы заполнения (под баннером висела чужая «Занято 5 из 6»), возврат ненулевого
   остатка в слоты («Свободно 3 модуля» рядом с каталогом «добавлять некуда»), обрыв перерисовки
   каталога (карточки прошлого поста с живыми обработчиками). Причина одна: проверялся объект
   контекста, а не доставка в DOM. Для баннера этот урок в том же коммите уже выучили — на
   остальные три не распространили. Теперь у каждого обещания есть убивающая мутация.

**Проверено мной лично в браузере** (воспроизводил честно — через сохранённый шаблон с
несуществующим артикулом, каталог не трогал):
```
артикул пропал  → ровно 1 ошибка; поле «Накладка недоступна — выберите замену»;
                  слоты 3 из 3; полоса и превью пусты; «Сохранить» заблокировано
выбрали замену  → 0 ошибок; «Занято 3 из 3»; превью вернулось; «Сохранить» активно
frameId пустой  → своя формулировка «Накладка поста не выбрана»; value поля ПУСТОЙ
                  (подмены нет); механизмы целы; сохранение заблокировано
новый пост      → 0 ошибок, накладка по умолчанию, «Занято 0 из 3» — не задет
```
Две мутации прогнал сам, обе краснеют.

**Найдено, но НЕ чинилось — это пункт 2.1 плана, Этап 2.** Кнопка «Лист монтажника» в том же окне
не блокируется (`js/app.js:3343`, `index.html:330`): окно только что запретило сохранять, а
документ печатает фантомный пост «до 1 мод.» вместо шести, теряет 4 механизма из 5, даёт **двум
модулям один адрес «1»** и молча выбрасывает строку «Накладка». Коммит `6e096c3` сделал
расхождение МЕНЕЕ заметным: раньше фантом был виден и в превью, и в документе.

## ⏭ 0.2 НЕ НАЧАТ — постановка готова, кончился лимит

Общий стенд `vm` + DOM-шим в `tests/helpers/`. Задача составлена целиком, суть:

- стенд уже собирался **минимум пять раз** (в тестах репозитория и трижды заново в scratchpad
  проверяющими агентами) и каждый раз выбрасывался; различия между копиями уже приводили к ложным
  выводам;
- исполнитель обязан **найти все копии сам** (список от меня был бы неполным — именно неполные
  списки в моих постановках порождали края), перевести на общий хелпер **ВСЕ без исключения**,
  иначе получим шестую версию;
- сохранить свойства, на которых держались настоящие находки; одно известно — шим `<select>`
  соблюдает спеку: присвоение `.value` отсутствующей опции снимает выбор. Остальные найти самому;
- хелпер должен быть **удобнее, чем собрать стенд заново**, иначе следующий агент соберёт шестой;
  в шапке — пример «поведенческий тест на функцию из app.js в пять строк»;
- главное требование: **ни один тест не ослаб**, таблица «мутация → красных до → красных после»
  по всем переведённым. Известные мутации перечислены в постановке;
- `js/*.js`, `index.html`, `css/` не трогать вообще.

## Прочее

`git stash@{0}` — недоделанный перенос строки предупреждения в статус (пункт 3.3), прервано
лимитом, один тест был красным. Stash цел и применим на `main`.


### 2026-09-02 — Починка блокеров конструктора; решение по app.js; уборка веток

**`wip/frame-modules-from-catalog`, HEAD `6e096c3`, 785 тестов, запушена. В `main` НЕ влита.**

## Состояние веток — приведено в порядок

Было шесть, стало три локально и четыре на сервере:

| ветка | назначение |
|---|---|
| `main` | `7af57e7`, актуальная |
| `client` | снимок для заказчика (от 01.08, он до сих пор не видел работы за август) |
| `wip/frame-modules-from-catalog` | текущая работа |
| `origin/wip/room-assign-tolerance` | **только на сервере**, архив вырезанного допуска |

Удалены: `wip/room-no-room-visibility` (её код целиком в `main`, дифф пуст) и
`docs/clean-readme-artifact` (её правка README уже в `main`).

**Правило, которого я не соблюдал: влил ветку — сразу удалил.** И не заводить новую ветку там,
где достаточно перевести существующую: пара почти одинаковых `wip/room-*` появилась именно так.

## Ветка конструктора: блокеры починены, но починка НЕ ПРОВЕРЕНА

Двойная проверка по `58a0675` нашла два блокера **в самих починках**, оба закрыты в `6e096c3`.

**Корень был поучительный.** Правка «не подменять молча недоступную накладку» породила состояние,
которого в коде не существовало: `selectedFrame === undefined`. Раньше накладка ВСЕГДА была
объектом — пусть чужим. Окружающий код к дыре готов не был:

```js
// js/posts.js:410  frameLayout(undefined) → capacity 0 → rows [[1]] — фантомная накладка на 1 модуль
// js/catalog.js    compatibleMechanisms(undefined, all) → ВСЕ 435 механизмов, фильтр серии выключен
```

Отсюда: под честным баннером печаталась вторая, вымышленная ошибка про импосты; каталог обещал
«Свободно 3 модуля» и прятал все 435 карточек; превью рисовало 1 модуль из 5. Теперь в этом
состоянии от несуществующей накладки не считается ничего, слоты не трогаются (побочно
нейтрализовано уничтожение механизмов селектором), окно называет ровно одну причину.

Второй блокер — тест: держал только первый `render` и проверял баннер по объекту контекста, а не
по DOM. Обе выживавшие мутации теперь краснеют, одну я проверил лично.

**⚠️ Сама починка `6e096c3` двойного прохода НЕ проходила.** Прошлый раз именно повторная
проверка починок нашла два новых блокера — не пропускать этот шаг.

## ⚠️ МОЙ УРОК ПО ПОСТАНОВКЕ ЗАДАЧ — записан и в память

**Перечисляя в задаче конкретные места, я задаю границу правки, и агент закрывает ровно
перечисленное.** В задаче на недоступную накладку я написал «не подменять, показать причину, не
дать сохранить» — назвал ТРЁХ потребителей. В той же функции их СЕМЬ. Правило §7.1 я применил к
работе агента и не применил к собственной постановке.

**Как ставить впредь:** вместо списка мест давать ВОПРОС — «найди всех потребителей этой
величины в этой функции и соседних, перечисли и скажи по каждому, что он делает в новом
состоянии». Известные места давать как примеры с пометкой «список неполон». Отдельно
спрашивать: «какого значения раньше быть не могло и кто его теперь получит».

## РЕШЕНИЕ ВЛАДЕЛЬЦА ПО `app.js`: сначала тестируемость, не разбиение

Факты, на которых решали: **3989 строк, 201 функция верхнего уровня**; разбиение по PLAN 2.1
объявлено сделанным 27.07 при 1233 строках — файл вырос ВТРОЕ после этого. Причина: резали по
чистоте («ни state, ни DOM»), поэтому весь слой связок остаётся здесь и копится. Доминирующий
кластер — конструктор поста: 210 упоминаний, пять из двенадцати крупнейших функций
(`renderBuilder` 178 строк, `renderBuilderComposition` 94, `trapBuilderFocus` 85,
`renderBuilderSlots` 78, `openPostBuilder` 73). Второй — `renderProperties`, 177 строк.

**Ключевой вывод: размер сам по себе денежных ошибок не создаёт.** Их создаёт то, что этот слой
нельзя вызвать из теста, поэтому его покрывают структурными тестами по тексту исходника — а они
ловят удаление строки и не ловят изменение смысла. Этот класс за сутки дал минимум восемь находок.

**Выбрано: вынести стенд `vm` + DOM-шим в `tests/helpers/` как общий инструмент.** Поведенческий
тест должен стать дешевле структурного. Продакшн-код не трогать.

Отвергнуты пока: вынос конструктора (~600 строк, трогает денежный путь — делать под прикрытием
поведенческих тестов, то есть ПОСЛЕ), и переход на ES-модули со сборкой (меняет весь уклад,
противоречит текущему этапу «вёрстка без сборки» из CLAUDE.md).

**Постановка задачи готова, но НЕ запущена — не хватило лимита.** Суть: стенд уже собирался
минимум ПЯТЬ раз (в тестах репозитория и трижды заново в scratchpad проверяющими агентами) и
каждый раз выбрасывался. Исполнитель обязан найти все копии САМ, перевести на общий хелпер ВСЕ
без исключения, сохранить свойства, на которых держались находки (шим `<select>` соблюдает
спеку: присвоение `.value` отсутствующей опции снимает выбор), и доказать таблицей
«мутация → красных до → красных после», что ни один тест не ослаб.

## ПОРЯДОК РАБОТ

1. **Общий стенд в `tests/helpers/`** — постановка выше.
2. **Повторить двойную проверку по `6e096c3`** и влить ветку конструктора в `main`.
3. Неблокирующее по конструктору: молчание листа монтажника, взрыв-схемы, подсказки на плане и
   «Раскладки постов» КП о пропавшей накладке; незаблокированная кнопка «Лист монтажника» в том же
   окне; тупик на многорядных 14/21; авто-имя поста как третий читатель селектора.
4. Осталось в `main` от ветки комнат: лишний toast «вне комнаты» при нуле комнат; строка
   предупреждения прячется вместе с правой панелью при ширине ≤1100 px (замер мой), а кольца
   остаются; CSS-тест переживает опустошение объявлений.
5. **Предсуществующий дефект, найден проверкой, в `main` тоже:** накладка с `active:false` молча
   подменяется на ВТОРОЙ перерисовке, и это меняет цену — 27,48 EUR превращаются в 9,35 EUR.
6. Шаг A по комнатам — дискриминатор «стена между точкой и контуром», прототип замерен.


### 2026-09-01 — Ветка конструктора прошла двойную проверку; три блокера закрыты

**`wip/frame-modules-from-catalog`, HEAD `58a0675`, 783 теста, запушена. В `main` НЕ влита** — после правки блокеров повторной проверки НЕ было (§7.1: правку проверяет не тот, кто её сделал).

## Что подтвердила двойная проверка

**Деньги не изменились** — прогон по ВСЕМ 1631 накладке при обоих типах стен: 0 расхождений с `main`, суммы 909 433,81 EUR и 923 627,55 EUR совпали побайтово. Денежные модули не тронуты (сверено контрольными суммами). Исходный баг воспроизведён на `main` («Занято 3 из 5», состав на 3 модуля, 18,54 EUR) и отсутствует на ветке. `dataset.preferredFrameId` (жалоба заказчика 24.08) НЕ сломан — проверено тремя открытиями подряд на одном DOM. Селектор `#postSlotCount` за пределами конструктора не читается нигде: документы и смета берут раскладку от накладки.

## Три блокера — закрыты коммитом `58a0675`

1. **Молчаливая подмена накладки с потерей механизмов.** Артикул накладки поста ушёл из перезалитого прайса (рабочий сценарий проекта) → `frameProduct` даёт `undefined` → защита рядом перестаёт работать → фильтр откатывается на весь каталог (1631 позиция) → пост получает ЧУЖУЮ 1-модульную накладку вместо 6-модульной, 4 механизма из 5 исчезают, **«Сохранить» АКТИВНО** и записало бы это в шаблон. Теперь накладка не подменяется никогда: окно говорит, что она недоступна, механизмы сохраняются, сохранение заблокировано.
2. **Второй источник ёмкости.** `pickBuilderProduct` читал селектор, `renderBuilder` — настоящую накладку. Один клик «Заменить» на заполненной 8-модульной накладке уничтожал ТРИ механизма. Ёмкость вынесена в `builderCapacity()`.
3. **Слепой тест.** `postSlotCountWiring` был единственным из девяти структурных тестов без `tests/helpers/stripComments.js` и подменял проверку поведения проверкой текста: четыре мутации выживали на зелёных 780, включая закомментированное тело функции, единственной наполняющей селектор (а разметку тот же коммит опустошил — в браузере селектор остался бы пустым). Переписан поведенчески через `vm` + DOM-шим с настоящим каталогом.

**Урок, стоит запомнить:** шапка того теста утверждала «app.js в node не грузится, поэтому проверяем по тексту» — и это **опровергалось собственным репозиторием**: техника `vm` + DOM-шим уже применена в `tests/renderSummaryOrphanWiring.test.js`. Оба проверяющих агента собрали на ней стенд и получили все числа выше. Прежде чем объявлять что-то непроверяемым, искать, не решена ли задача рядом.

## Осталось по ветке (не блокирует, но записать)

- **Д4** — `||count` для многорядных 14/21: пост на `14690.01` (14 модулей) показывает «Занято 8 из 8», 6 механизмов выброшены, «Сохранить» заблокировано НАВСЕГДА («заполните все модули» физически невыполнимо). Достижимо только на данных извне — через интерфейс такой пост не создать. Заявление «ёмкость от настоящей накладки» на этих 18 артикулах ложно.
- **Д5** — зашитая «3» в `openPostBuilder` (`find(frame=>frameSlotCount(frame)===3)`, `defaultPostName(3)`) не следует за каталогом: уйдут 3-модульные накладки — «Новый пост» даст имя «Пост на 3 модуля» при 1-модульной накладке. Латентно (3-модульных 224 шт.).
- **Д6** — авто-имя поста считается от селектора — третий потребитель ёмкости помимо двух сведённых.
- **Продуктовый пробел (замер мой):** 18 многорядных накладок 14/21 недостижимы в конструкторе в принципе — список фильтруется по `frameSlotCount === count`, а у них `null`. Каталог несёт позиции, которые пользователь не может выбрать никогда. Было и до правки.

## ⚠️ ПОВТОРНАЯ ПРОВЕРКА ПОЧИНОК ЗАПУСКАЛАСЬ И ОБОРВАЛАСЬ

Обе проверки по коммиту `58a0675` (починки трёх блокеров) запущены и **оборваны пятичасовым
лимитом, не выдав отчётов**. Считать починки проверенными НЕЛЬЗЯ — запускать заново с нуля.

Единственное, что успел выдать независимый контроль до обрыва: **тождество `builderCapacity()`
прежней формуле подтверждено — 13 048 проверок, 0 расхождений.** То есть ёмкость на валидных
накладках не изменилась. Остальное (Б1 подмена накладки, Б3 покрытие, третий читатель селектора)
осталось непроверенным.

**Что проверено мной лично в браузере:** починка НЕ сломала нормальный путь. Пост сохранён и
открыт на редактирование заново — накладка `09673.01` на месте, 224 варианта в поле,
«Занято 3 из 3 · рамка заполнена», 3 слота, «Сохранить» активно, **ложного баннера «недоступна»
нет**. Новое условие не срабатывает там, где не должно.

Сценарий «артикул исчез из каталога» в браузере воспроизвести не удалось: правка массива
`EP_VIMAR_CATALOG.products` не действует, `product()` читает построенный при загрузке индекс.
Этот вход проверяется только стендом `vm` + DOM-шим, он поручён сабагентам.

**Грабля процесса, повторилась дважды за сессию:** `python -c "..."` в Bash съедает бэктики как
подстановку команд — куски текста в обратных кавычках исчезают из файла молча. Писать длинный
текст только через файл (Write в scratchpad + splice), а не инлайном.

## СЛЕДУЮЩИЙ ШАГ

1. **Повторить обе проверки по ветке** — блокеры чинились после прошлого прохода, непроверенными остались сами починки. Отдельно посмотреть в браузере баннер «Накладка поста недоступна» и плейсхолдер в поле: исполнитель прогонял его через заглушку, а в живом интерфейсе поле оборачивает `EPPicker`.
2. Влить в `main`.
3. Затем — Д4/Д5/Д6 и три находки, оставшиеся в `main` от ветки комнат (Д3 лишний toast, Д6 строка предупреждения прячется на ширине ≤1100 px, Д7 слабый CSS-тест).


### 2026-08-31 (конец сессии) — Ветка комнат ВЛИТА в main. Ветка конструктора ждёт состязательного прохода

## ✅ Ветка комнат влита: `main` = `7af57e7`, 774 теста, запушено

`wip/room-no-room-visibility` слита в `main` через `merge --squash` (в её истории был
промежуточный несогласованный коммит `9cb8671`, тащить его незачем). Конфликт в `HANDOFF.md`
разрешён в пользу ветки — там лежал журнал `main` плюс новая запись; дерево после слияния
совпало с веткой байт в байт, 774/774.

В `main` уехало: видимость объектов вне помещений; правило «в какой комнате точка» в одной
функции; контракт `refreshAfterRoomAssignments`; **починка потери стен при перезагрузке**;
честный комментарий вместо обещания вырезанного допуска.

**В сообщении коммита явно объявлено, что слияние МЕНЯЕТ ДЕНЬГИ** (40.52 EUR против 51.58 EUR на
входах, где `componentAt` даёт −1) и почему это исправление противоречия, а не регресс.

### Что осталось незакрытым в самом `main` (неблокирующее, из состязательного прохода)

- **Д3** — пятая формулировка «вне помещений»: `addPending` шлёт toast «Объект размещён вне
  комнаты» на КАЖДЫЙ размещённый объект, пока комнат в проекте нет. Ровно тот шум, который
  правило `isOutsideRooms` объявило недопустимым (метка при `roomCount==0` гасится).
  `renderProperties` пишет «Не назначена» по тому же старому критерию.
- **Д6 (замер мой, в браузере)** — `#lightingSummary` лежит внутри `.right-panel`, которую
  `css/styles.css` прячет `@media(max-width:1100px){.right-panel{display:none}}`. При 1090 px:
  панель `display:none`, строка предупреждения в DOM есть, высота 0, а кольца на плане горят.
  На ноутбуке 1366 с масштабом 125 % пользователь видит метку и нигде не видит объяснения.
  Требует решения владельца: перенести строку, дублировать её или показывать иначе.
- **Д7** — `noRoomStyleWiring` переживает опустошение объявлений при сохранённом селекторе:
  `.plan-icon.no-room{z-index:6}` оставляет прогон зелёным, а метка с экрана исчезает. Тест
  держит существование селектора и непустоту блока, но не то, что кольцо реально объявлено.

## ⚠️ ГЛАВНОЕ НА СЛЕДУЮЩУЮ СЕССИЮ: `wip/frame-modules-from-catalog` НЕ ПРОВЕРЕНА

**HEAD `cbd639e`, 737 тестов, запушена, от СТАРОГО `main` (`1042c4a`) — перед работой подтянуть
новый `main` (`7af57e7`).**

Это починка бага, найденного владельцем вживую (селектор «5 модулей» давал окно, противоречащее
себе). Что сделано и как — в записи «31.08 (ночь)» ниже.

**Двойного прохода по ней НЕ БЫЛО.** Claude проверил её лично в браузере и тестами, но
состязательной проверки не проводилось, а в этом проекте она находила дефект практически каждый
раз, включая работы, которые уже считались готовыми.

**Порядок действий:**
1. Подтянуть в ветку актуальный `main` (`7af57e7`), прогнать `npm test`.
2. Прогнать по ней **`ep-verify` и `ep-adversary` параллельно, с разными заданиями** (§7.2).
   Правка трогает подбор суппорта и монтажной коробки, то есть в итоге деньги — проверять как
   денежную. Отдельные направления для состязательного: сохранённые посты с модульностью, которой
   больше нет в списке; многорядные накладки 14/21 (`frameSlotCount` → `null`); механизм
   `dataset.preferredFrameId` (за ним реальная жалоба заказчика от 24.08); известная оговорка —
   `pickBuilderProduct` всё ещё читает число из селектора для предварительного фита.
3. Разобрать находки, починить, **влить в `main`**.


### 2026-08-31 (ночь) — Две ветки в работе: видимость комнат доведена, баг конструктора починен

Обе запушены. **Ни одна в `main` не влита.**

| ветка | HEAD | тесты | статус |
|---|---|---|---|
| `wip/room-no-room-visibility` | `871fe34` | 773 | двойная проверка пройдена, осталось 5 неблокирующих находок |
| `wip/frame-modules-from-catalog` | `d83b00c` | 737 | от `main`, готова к слиянию после ревью |

## Ветка конструктора — баг, найденный владельцем в живом интерфейсе

Владелец выбрал «Количество модулей рамки» = 5 и получил окно, противоречащее себе: накладка
осталась `09673.01` на 3 модуля, полоса писала «Занято 3 из 5 · свободно 2 модуля», каталог
справа — «шире свободного места (0 модулей)», суппорт и коробка подобраны под настоящие 3.

**Причин две.** Селектор был зашит в `index.html` списком 1..8, тогда как накладок на 5 модулей
в каталоге **нет вообще** (замер по 1631 накладке: 1 — 158, 2 — 360, 3 — 224, 4 — 319, 6 — 93,
7 — 199, 8 — 260; остальные 18 — многорядные 14/21, `frameSlotCount` даёт `null`). А фильтр
накладок при пустой выборке молча откатывался на весь каталог (`matchingFrames.length?...:allFrames`),
оставляя прежнюю накладку и не говоря ни слова.

**Решение владельца:** список модульностей строится ИЗ КАТАЛОГА. Плюс ёмкость сведена к одному
источнику — настоящей выбранной накладке; от неё считают фит, остаток, лимит карточек каталога и
полоса заполнения. Край закрыт: сохранённый пост с исчезнувшей модульностью открывается, его
фактическая ёмкость добавляется в список ДО присвоения `value`.

**Проверено вживую:** селектор даёт 1, 2, 3, 4, 6, 7, 8; при выборе 4 накладка меняется на
4-модульную, полоса пишет «свободно 1 модуль», каталог — то же самое.

**Оговорка, не блокер:** `pickBuilderProduct` при клике по карточке ещё читает число из селектора
для предварительного фита; следом `renderBuilder` пересчитывает по настоящей накладке, а
«Сохранить» заблокировано до валидной раскладки — на экран и в сохранение не попадает.

## Ветка комнат — двойная проверка ПРОЙДЕНА, 7 + 5 находок

**`ep-adversary` нашёл 2 блокирующие, `ep-verify` вынес «утверждение НЕ подтверждаю».** Обе
блокирующие и главное опровержение закрыты коммитом `871fe34` (768 → 773 теста):

1. **Критерий метки у `compactIcon` не был покрыт ничем** — а именно он решает, что видно на
   экране: любое изменение числа комнат идёт через `renderAll`, а тот пересоздаёт иконки,
   выбрасывая всё, что проставил `syncNoRoomClass`. Инверсия критерия оставляла 768/768 зелёными,
   тогда как та же инверсия у `syncNoRoomClass` давала 5 падений.
2. **Тест контракта не держал сами вызовы** — удаление `refreshAfterRoomAssignments` из любого из
   15 мест проходило зелёным. Закрыто переписью числа вызовов, не пятнадцатью проверками.
3. **★ Ветка ВСЁ-ТАКИ меняет деньги относительно `main`, и это правильно.** Установлено мной по
   коду: в `main` `getRoomForPoint` имеет `if(target<0)return null`, а копия правила внутри
   `recalculateRoomAssignments` — **НЕ имеет**. То есть подсветка при перетаскивании говорила
   «вне помещений», а фактическая привязка в тот же момент приписывала объект комнате, чей seed
   тоже заблокирован (`-1 === -1`). Объединение правила выбрало защищённый вариант. Цена отличия
   на настоящем каталоге: `main` 40.52 EUR против ветки 51.58 EUR на входе «6 отрезков-штрихов в
   блоке 80×80 px, подпись-комната с seed внутри блока». Теперь есть тест на настоящей
   `buildSpaceComponents` и честный комментарий в коде. **Заявление «ветка не меняет ни одной
   цифры» было неверным — при слиянии описывать честно.**

### Осталось по ветке комнат — 5 неблокирующих находок

- **Д3** — пятая формулировка «вне помещений»: `js/app.js` `addPending` шлёт toast «Объект
  размещён вне комнаты» на КАЖДЫЙ размещённый объект, когда комнат в проекте ещё нет. Это ровно
  тот шум, который ветка объявила недопустимым (`isOutsideRooms` гасит метку при `roomCount==0`).
  Есть и в `main`, регрессом не является. Плюс `renderProperties` пишет «Не назначена» по старому
  критерию.
- ~~**Д4**~~ — ЗАКРЫТО коммитом af78b59 на ветке комнат. Было: комментарий в `compactIcon` обещает вырезанный допуск («даже с допуском у границы») и
  неверно называет toast вместо `updateStatus`. `index.html` бьёт кэш `js/config.js`, который
  побайтово идентичен `main`.
- ~~**Д5**~~ — **ЗАКРЫТО** коммитом ed6aff8 на ветке комнат: три пути стен теперь передают в контракт scheduleSave, четвёртого несохраняющего места нет, тест стережёт класс целиком, проверено вживую (стены переживают F5). Было — ПОДТВЕРЖДЁННАЯ ПОТЕРЯ ДАННЫХ, замер мой, в браузере: Нарисовал инструментом «Стены»
  два отрезка -> localStorage ПУСТ (ни одного ключа) -> F5 -> **0 стен**, «Элементов: 0 · Постов: 0
  · Комнат: 0». Обрисованный вручную план исчезает от перезагрузки, пока пользователь не сделает
  что-то другое, что запускает сохранение (например, поставит пост — тогда стены уедут в снимок
  заодно). Есть и в main, регрессом ветки не является. Технически: три из 15 мест контракта не сохраняют (`addWallPoint`, `autoTracePlan`,
  `clearAutoTraceBtn`): нарисовал стены, нажал F5 — стены пропали. Есть и в `main`, но комментарий
  в `addWallPoint` утверждает «тот же контракт, что у `addRoomLinePoint`», а тот сохраняет.
  **Проверить и завести отдельной задачей — это потеря пользовательских данных.**
- **Д6 (проверено мной вживую)** — `#lightingSummary` лежит внутри `.right-panel`, которую
  `css/styles.css` прячет `@media(max-width:1100px){.right-panel{display:none}}`. Замер при 1090 px:
  панель `display:none`, строка предупреждения в DOM есть, высота 0; кольца на плане горят.
  На ноутбуке 1366 с масштабом 125 % пользователь видит метку и не имеет объяснения нигде.
- **Д7** — `noRoomStyleWiring` переживает опустошение объявлений при сохранённом селекторе:
  `.plan-icon.no-room{z-index:6}` оставляет 768 зелёными, а метка с экрана исчезает. Тест держит
  существование селектора и непустоту блока, но не то, что кольцо реально объявлено.

### Что подтверждено проверками как ЗАКРЫТОЕ (с доказательствами)

Места записи `roomId` (их ровно два, оба зовут `syncNoRoomClass`); копия поста не наследует чужой
`roomId` (`EPPosts.placementFields` — белый список); обратная совместимость сохранённых проектов
(снимок не несёт полей допуска, `init` пересчитывает); каскад CSS метки (все правила, способные
перебить кольцо, проверены); `stripComments` не врёт на текущем входе; контракт из 15 мест сверен
построчно с `main` — подмен `scheduleSave`/`persistProject` нет; карта пространства строится один
раз на пересчёт (1/1/1/1 при 1/10/50/200 объектах).

## ПОРЯДОК РАБОТ В СЛЕДУЮЩЕЙ СЕССИИ

1. **Ветку конструктора** (`wip/frame-modules-from-catalog`) — прогнать `ep-verify` +
   `ep-adversary` и влить: она мелкая, независимая и чинит живой баг.
2. **Ветку комнат** — доделать Д4 и Д7 (дешёвые, честность кода и теста), решить по Д3 и Д6,
   завести отдельную задачу на Д5. Затем слияние squash (в истории есть промежуточный
   несогласованный `9cb8671`). **HANDOFF.md на ветке комнат уже подтянут к версии `main` —
   не потерять при слиянии.**
3. **Шаг A** — дискриминатор «стена между точкой и контуром». Замеренный прототип и подводный
   камень `segmentsIntersection` записаны в записи от 31.08 (вечер) ниже.
### 2026-08-31 (вечер) — Развилка закрыта владельцем; ветка сужена до видимости и доведена

**`wip/room-no-room-visibility`, HEAD `a4ac9b2`, 768 тестов зелёные. Ветка `wip/room-assign-tolerance`
(`d7e4f0c`) НЕ выброшена — остаётся исходником для шага A.**

## Решение владельца по развилке «каким должен быть дискриминатор»

Выбран вариант **D, затем A**:
- **D (сделано в этой сессии)** — вырезать допуск, влить в `main` только видимость;
- **A (следующий блок)** — дискриминатор «есть ли стена между точкой и контуром»: зонд от точки
  до ближайшей точки контура плюс ~1 px внутрь, проверка пересечения со стенами; стена,
  проходящая через саму точку, не считается.

Отвергнуты: **B** (репрезентативная точка вместо центроида — чинит только вогнутые комнаты,
оставляет Б1 и Б2) и **C** (привязка при постановке — требует сперва стабильных id комнат,
сейчас `uid("room_")` переприсваивается при каждом пересчёте; 4-6 задач, отдельный блок).

**Прототип A замерен ДО решения, не предположен.** Зонд даёт правильный и устойчивый ответ во
всех мотивирующих случаях, где компонента связности была бесполезна:

```
центр РОВНО на верхней/левой/нижней/правой стене -> ПРИВЯЗАН (все четыре одинаково)
коридор, 8 px за глухой стеной                   -> нет
в дверном проёме, 8 px ниже контура              -> ПРИВЯЗАН
у глухого куска той же стены, 8 px               -> нет
Г-образная комната, пост в Гостиной за ножкой    -> нет  (seed не используется вовсе)
сдвиг всего плана на 5 px                        -> ответы не изменились
```

Вся математика уже есть в `geometry.js` (`closestPointOnSegment`, `segmentsIntersection`) —
второй реализации заводить не придётся. **Подводный камень, зафиксировать для исполнителя:**
`segmentsIntersection` считает касание концами пересечением (параметры t и u в отрезке [0;1]),
поэтому зонд обязан игнорировать стену, проходящую через саму точку (иначе объект РОВНО на
контуре отвергается), и обязан заходить за контур внутрь (иначе объект за стеной принимается —
пересечение попадает ровно в конец зонда). Тесты «ровно на стене» и «8 px за стеной» обязательны.

**Поправка к записи прошлой сессии:** `componentAt` смещается ВВЕРХ-ВЛЕВО, а не вниз-вправо
(внешний цикл по oy от -radius, внутренний по ox от -radius; первый кандидат — `gx-radius, gy-radius`).
На вывод это не влияет — сторона всё равно решается порядком обхода, а не геометрией.

## Что сделано по шагу D — три коммита

- **`9cb8671`** — допуск вырезан целиком: `nearestRoomWithinTolerance`, `EPConfig.roomEdgeTolerance`,
  ветвь допуска в `resolveRoomForPoint`, алиас `distancePointToSegment`, тесты допуска. Оставлено
  то, что деньги не трогает: метка «вне помещений» синхронно с записью `roomId`, строка
  предупреждения, единый критерий `isOutsideRooms`, и сведение правила «в какой комнате точка»
  в ОДНУ функцию вместо двух копий. Дифференциально против `main`: 120 планов, 15 816 точек,
  0 расхождений; тот же харнесс против прежнего HEAD даёт 3074 расхождения.
- **`6fb5e25`** — контракт обновления после смены привязки сведён в
  `refreshAfterRoomAssignments(paint, save)`. Пять мест из пятнадцати не звали `renderProperties`:
  кольцо на плане горело, а карточка поста показывала прежнюю комнату и прежнюю цену. Переведены
  ВСЕ 15 мест, не только сломанные — иначе остались бы две системы.
- **`a4ac9b2`** — три пробела покрытия: поведенческий тест проводки предупреждения в
  `#lightingSummary`; структурный тест пяти CSS-правил метки, берущий имена классов ИЗ `app.js`
  (переименование класса в JS без правки CSS краснеет); вырезание комментариев вынесено в
  `tests/helpers/stripComments.js` и подключено ко всем семи `*Wiring`-тестам.

731 (main) -> 768 тестов.

## ⚠️ МОЯ ОШИБКА, которую надо не повторить: `git checkout <файл>` при пустом индексе

После мутационной проверки D1 я откатил `js/app.js` командой `git checkout js/app.js`. Индекс был
пуст, поэтому файл вернулся не к рабочему состоянию, а **к HEAD — то есть к версии с допуском**.
Обе мои проверки (757/757 и `grep` по остаткам допуска) прошли ДО этого отката, поэтому я его не
увидел и закоммитил `9cb8671` несогласованным: `config.js`/`roomAssign.js`/тесты без допуска,
`app.js` с ним. Три «доисторических» падения, о которых честно доложил следующий агент, — это оно.
Восстановлено внутри `6fb5e25`, ветка не была запушена, в `main` ничего не попало.

**Правило на будущее: мутации откатывать копированием из резервной копии в scratchpad, а не через
git. И повторять `npm test` ПОСЛЕ отката, а не только до мутации.**

## Отдельная грабля ветки: устаревший HANDOFF.md

`wip/room-assign-tolerance` (а значит и эта ветка) несла **старую копию `HANDOFF.md`** — на 366
строк короче, чем в `main`. Слияние без внимания стёрло бы весь журнал, накопленный в `main` после
ответвления. Подтянул версию `main` перед записью. **Проверять это при каждом слиянии долгоживущей
ветки.**

## Проверено мной лично в браузере (то, что сабагенты не могут)

`localhost:8420`, чистый localStorage, консоль без единой ошибки, загружены `app.js?v=20260831.2`,
`EPRoomAssign` = только `isOutsideRooms`, `roomEdgeTolerance` отсутствует.

Замер computed-стилей настоящим курсором, три состояния метки:

```
.plan-icon.post   -> тени кольца нет, ::after content: none
+ .no-room        -> 0 0 0 2px rgba(224,138,43,.55) + border #e08a2b + ::after "!" на оранжевом
+ :hover          -> кольцо СОХРАНЕНО + brightness(1.08); проверено настоящим курсором, элемент
                     действительно в состоянии :hover
+ .dragging       -> кольцо СОХРАНЕНО + подъёмная тень 0 12px 26px rgba(16,42,67,.42)
.lighting-orphan-note -> фон #fdf1e2, текст #8a4a12, рамка 1px #f0cfa2, радиус 8px
```

Сквозной сценарий через настоящий UI (пост из конструктора, два поста на плане, стены коробкой,
подпись-комната внутри):

```
комнат нет, два поста          -> меток нет, строки предупреждения нет   OK (критерий roomCount>0)
комната есть, пост внутри      -> "plan-icon post"         (кольца нет)   OK
комната есть, пост снаружи     -> "plan-icon post no-room" (кольцо)       OK
строка: «Вне помещений: 1 — отмечены на плане. Из них постов: 1 — их схема электрики
считается по проекту. Перетащите объект в комнату или подвиньте контур.»  OK
```

**Переход через границу «комнат стало 0 / стало 1» — ПРОВЕРЕН ВЖИВУЮ** (это оставалось последним
непроверенным узлом; синтетический клик по подписи комнаты инструмент «Удалить» не срабатывает,
понадобился настоящий клик мышью):

```
комната есть, два поста        -> "plan-icon post" / "plan-icon post no-room", строка есть
удалили ЕДИНСТВЕННУЮ комнату   -> Комнат: 0, ОБЕ иконки без no-room, строки НЕТ      OK
поставили комнату обратно      -> Комнат: 1, метка и строка вернулись на место       OK
```

Критерий `roomCount > 0` работает в обе стороны, метки не «залипают» после исчезновения комнат.

## ЧТО ОСТАЛОСЬ ДО СЛИЯНИЯ

1. **Дождаться и разобрать два фоновых прохода**, запущенных в конце сессии и НЕ дождавшихся
   ответа: `ep-verify` (главное утверждение на проверку — «ветка не меняет ни одной цифры в
   деньгах относительно `main`») и `ep-adversary` (края: все места записи `roomId`, переход
   «комнат стало 0/1», четвёртая формулировка «вне помещений» в toast, обратная совместимость
   сохранённых проектов, атака на новые тесты). **Первый запуск обоих проходов ОБОРВАЛСЯ на пятичасовом лимите, не выдав ни одной находки; после сброса лимита оба перезапущены с нуля. Пока их отчёты не разобраны — ветку проверенной НЕ считать.**
2. Только после этого — слияние в `main` (удобно squash: в истории ветки есть промежуточный
   несогласованный коммит `9cb8671`).
3. Затем шаг **A** — дискриминатор «стена между точкой и контуром», отдельным блоком, с
   переиспользованием `wip/room-assign-tolerance` как исходника.

**Известный побочный эффект `6fb5e25`, владельцу на решение:** пять путей теперь зовут
`renderProperties`, поэтому несохранённый черновик имени/площади комнаты в панели свойств будет
теряться чаще (`finishDrag` комнаты, `addWallPoint`, `addRoomLinePoint`, `removeLastRoomLinePoint`,
`removeRoomLine`). Это та же открытая развилка «сохранять по вводу или беречь черновик при
перерисовке», что записана 28.08. Не решал и не маскировал.


### 2026-08-31 (финал сессии) — Ветка доведена, но НЕ ВЛИТА: денежный дефект на вогнутых комнатах

**`wip/room-assign-tolerance`, HEAD `d7e4f0c`, 776 тестов зелёные, запушена. В `main` НЕ вливать.**

Закрыто восемью коммитами: `ReferenceError`; порядок ветвей стал условным (компонента
связности ПРОВЕРЯЕТ кандидата от допуска, а не конкурирует с ним); критерий метки сведён в
одну `EPRoomAssign.isOutsideRooms` (копий было ТРИ — третья жила в счётчике предупреждения без
учёта числа комнат); поведенческое покрытие метки через `vm` + DOM-шим; честный текст
предупреждения (общий счёт по постам и устройствам — чтобы совпадать с кольцами на плане,
денежная оговорка только про посты); `renderSummary` в `addWallPoint`; CSS `.no-room.dragging`;
guard допуска (`undefined`/`NaN`/`Infinity` больше не снимают границу молча). Тесты 739 → 776.

**Независимый контроль подтвердил своими прогонами:** правило на настоящей геометрии
(`buildSpaceComponents`, не рукодельные карты); деньги на настоящем каталоге — 51.58 € проходная
против 40.52 € два выключателя, разница 11.06 €; дифференциально против `main` 12 000 точек на
200 планах — 195 расхождений, ВСЕ в зоне допуска, необъяснённых ноль; единственность критерия
метки; честность текста во всех четырёх состояниях; производительность (объект внутри контура
не платит ни прохода по рёбрам, ни проверки компоненты); 12 собственных мутаций.

## ★★ ТРЕТИЙ ПРОХОД: ЗАМЫСЕЛ ПРОВЕРКИ КОМПОНЕНТЫ НЕВЕРЕН В ОСНОВЕ

Помимо блокера с вогнутыми комнатами (ниже), состязательный проход показал, что сама идея
«проверять кандидата компонентой связности» опирается на величину, которая **по построению не
может ответить на заданный вопрос**.

**Б1. `componentAt` для точки НА стене не различает сторону.** Он берёт первую свободную клетку,
обходя `oy=-r..r, ox=-r..r`, то есть смещается ВНИЗ и ВПРАВО. Стены снапятся к сетке
(`gridStep=10`), `spaceGrid` кладёт origin кратно `cell`, обе соседние клетки заблокированы —
и «сторона» определяется порядком обхода, а не геометрией. Прогон (комната запечатана,
подпись-комната в коридоре):
```
центр РОВНО на верхней стене (200,100) → Спальня          ← случайно верно
центр РОВНО на левой  стене (100,200) → Спальня           ← случайно верно
центр РОВНО на нижней стене (200,300) → ВНЕ ПОМЕЩЕНИЙ     ← мотивирующий случай НЕ чинится
центр РОВНО на правой стене (300,140) → Коридор(подпись)  ← ЧУЖАЯ комната, БЕЗ метки
```
Сдвиг всего плана на 5 px (стены попадают в середину клеток) меняет ответы. **Мотивирующий
случай — центр ровно на контуре — это ровно тот вход, где `componentAt` бесполезен.**

**Б2. Проверка включается только при наличии grid-комнаты.** `roomResolveContext`
(`js/app.js:851`) строит карту лишь `if(gridRooms.length)`, а `resolveRoomForPoint`
(`js/app.js:887`) при `!ctx.map` принимает кандидата без проверки. Значит в проекте из одних
контурных комнат защиты нет, а добавление одной подписи-комнаты где угодно на плане меняет
привязку НЕПОДВИЖНЫХ постов:
```
только контурная Спальня:             p1=R1, p2=R1   → 51.58 € (переключатели)
+ подпись-комната в коридоре:         p1=R1, p2=null → 40.52 € (выключатели)
```
Посты не двигали. Цена и физическая схема зависят от постороннего ярлыка.

**Вывод: правило нуждается не в доводке, а в другом дискриминаторе.** Компонента связности не
годится: она не определена для точки на стене — то есть именно там, где нужна.

**Б3. `addWallPoint` не зовёт `renderProperties`.** Комментарий правки утверждает «тот же
контракт, что у `addRoomLinePoint`», но скопированы 3 вызова из 5. Карточка выделенного поста
показывает прежнюю комнату и прежнюю ЦЕНУ, пока кольцо на плане уже горит. Тем же неполным
списком страдают `js/app.js:2507, 2519, 2525`.

**Б4. Строку предупреждения можно отключить от интерфейса** — убрать `+orphanObjectsWarningHtml()`
из `renderSummary` (`js/app.js:1642`), и 776 тестов остаются зелёными. Проводка второго
потребителя не застолблена, хотя для `addWallPoint → renderSummary` тест есть.

**Б5. Все пять новых CSS-правил можно удалить — 776 зелёных.** Это и есть ответ на вопрос «что
шим не поймает, а браузер покажет»: шим проверяет лишь попадание строки `"no-room"` в
`classList`. Переименование класса, опечатка в селекторе, потеря правила при слиянии — метка
исчезает с экрана молча.

## ★ БЛОКЕР — снова дефект замысла основного агента

**Проверка компоненты не работает на ВОГНУТЫХ комнатах.** `js/app.js:888`:
```js
const seed=near.seedX!=null?{x:near.seedX,y:near.seedY}:polygonCentroid(near.polygon);
```
У полигональных комнат `seedX/seedY` — это ЦЕНТРОИД. У Г- и П-образной комнаты центроид лежит
**вне её собственного контура**, как правило в соседнем помещении. Тогда `componentAt(seed)`
возвращает компоненту ЧУЖОЙ комнаты, сравнение проходит, и кандидат принимается **сквозь глухую
стену**. Проверено лично: центроид контура
`(100,100)(500,100)(500,200)(200,200)(200,500)(100,500)` = `{266.7, 266.7}`,
`pointInPolygon` внутри своего контура → **false** (у прямоугольника → true).

Деньги на настоящем каталоге: пост в Гостиной за глухой стеной в 8 px от контура Г-образной
Спальни → привязывается к Спальне → **51.58 € вместо 40.52 €**. Против `main` это РЕГРЕСС: там
он получал `roomId=null` и считался отдельной партицией.

**Это буквально тот дефект, ради починки которого делался `f2bd219`** — закрыт только для
выпуклых комнат. Оба теста «проём/стена» построены на прямоугольниках, поэтому дефект зелёный.
Вогнутые контуры не экзотика: `roomsFromLines` и `roomSegmentation` выдают Г-образные помещения
регулярно, и seed им ставится тем же `polygonCentroid`.

**Минимум для повторной сдачи:** seed кандидата обязан быть точкой ВНУТРИ его контура
(репрезентативная точка полигона, не центроид), либо проверка компоненты должна опираться на
другое свидетельство. Плюс тест на вогнутый контур, чей центроид лежит вне него.

## Прочие находки контроля (не блокеры)

- **Структурные тесты удовлетворяются вызовом ВНУТРИ комментария.** `/syncNoRoomClass\s*\(/`
  матчит закомментированный код: мутация «закомментировать вызов» оставляет 776 зелёными, а
  «удалить строку» — краснеет. Класс «выключили, оставив в комментарии» не держится ни одним
  `*Wiring.test.js`. Шапки этих тестов обещают больше, чем проверяют.
- **Отсечка отрицательного допуска — эквивалентный мутант:** убрать `|| tolerance < 0` ничего не
  меняет (`d > -1` истинно всегда → все комнаты пропускаются). Не дефект, но и не покрыто.
- **Четвёртая формулировка «вне помещений» в словах** — toast `js/app.js:1153` использует только
  `room==null`, без учёта числа комнат. Есть и в `main`, регрессом ветки не является.

## ЧТО ДЕЛАТЬ ДАЛЬШЕ

1. **Починить блокер** — репрезентативная точка вместо центроида + тест на вогнутый контур.
2. Закрыть Р2 (регэкспы структурных тестов должны игнорировать комментарии) и снять из их шапок
   невыполнимые мутационные обещания.
3. **Повторить обе проверки** и только потом вливать.
4. Визуальную часть CSS я проверил лично замером `box-shadow` в браузере: кольцо метки и
   подъёмная тень при переносе сочетаются, от положения курсора не зависят; на плане без комнат
   строки предупреждения нет. Это единственное, что сабагенты проверить не могут.

### 2026-08-31 — Второй состязательный проход: 5 дефектов, главный — от МОЕГО решения

**`wip/room-assign-tolerance`, HEAD `4eb486e`, 752 теста. В `main` НЕ вливать.**

**Д1 (денежный, главный) — регресс, внесённый перестановкой ветвей.**
Порядок «контур → допуск → grid» выбрал основной агент, обосновав: grid-совпадение это «в той же
компоненте связности, что и подпись», а компонента через проёмы накрывает всю квартиру, значит
свидетельство слабейшее. **Обоснование верно НЕ ВСЕГДА, а код применяет порядок безусловно.**
Когда полигональная комната замкнута и компонента точки ДРУГАЯ, компонента — прямое
доказательство «объект НЕ в этой комнате», и оно выбрасывается.
Замер (живой `resolveRoomForPoint` в `vm`, замкнутая Спальня + коридор отдельной компонентой):
```
пост в коридоре, 8 px от контура спальни  | main: Коридор | HEAD: Спальня  ← РЕГРЕСС
пост в коридоре, 11 px                    | main: Коридор | HEAD: Спальня  ← РЕГРЕСС
пост в коридоре, 13 px                    | main: Коридор | HEAD: Коридор
```
Деньги (настоящий `planByRooms` + `plan`, два поста одной группы): правильно 2×25.79 = **51.58 €**
(проходная), регресс 2×20.26 = **40.52 €** (два независимых выключателя — ещё и другой монтаж).
**Правка не убрала перехват, а развернула его**: раньше grid-подпись перехватывала объект на
контуре полигона, теперь допуск полигона отбирает полосу 12 px вокруг каждого контура у
grid-комнат. Тест `roomResolveRule` этого не ловит: он использует карту, где вся плоскость —
ОДНА компонента, а случай «компоненты разные» не покрыт вовсе.
**Вывод: порядок ветвей нельзя решать безусловно. Нужно правило, учитывающее, противоречит ли
grid-свидетельство допуску (совпадает ли компонента точки с компонентой комнаты-кандидата).**

**Д2** — значок на плане и счётчик «⚠ Вне помещений: N» расходятся на пути `addWallPoint`
(`js/app.js:2379` не зовёт `renderSummary`). Метка теперь ставится сразу, счётчик — нет. До
правки было невидимо: обновлять на плане было нечего. Край «починено в одном месте, не в
соседнем эквивалентном»; в этом же файле есть правило «экран и документ противоречить не могут».

**Д3** — предупреждение считает устройства наравне с постами (`js/app.js:1589`), а его текст
«схема электрики у них считается по проекту» верен ТОЛЬКО для постов: `lightingFor(posts)`
собирает места лишь из постов, `roomId` устройства ни в одном денежном пути не участвует.
Три розетки вне комнат дают ложную денежную тревогу.

**Д4** — CSS: правило «метка переживает другое состояние» применено к `:hover`, но не к
`.dragging` (0-2-0, ниже `.no-room`). Кольцо исчезает при переносе, если курсор вне иконки;
если внутри — пропадает подъёмная тень. Нужен `.plan-icon.no-room.dragging`.

**Д5 (латентный)** — `js/roomAssign.js:40` `d > tolerance`: при `undefined`/`NaN` отсечка молча
снимается и берётся ближайший контур на всём плане (комната в 500 px). `null` ведёт себя
противоположно — асимметрия и есть признак отсутствующей проверки. Живого пути нет.

**ОТДЕЛЬНО — метка «вне помещений» поведенчески не покрыта НИЧЕМ.** Мутации, оставляющие
752/752 зелёными: инверсия критерия (метка горит ровно на объектах, которые В комнате) и слом
селектора (`data-id` → `data-kind`, узел не находится никогда, вся правка мертва).
`roomNoRoomLabelWiring` держит только НАЛИЧИЕ вызовов, не поведение.

**Что проход ПОДТВЕРДИЛ закрытым:** `ReferenceError` (мутация «убрать алиас» роняет 4 теста);
записей `roomId` ровно две и обе синхронизируют метку; селектор не может попасть в подпись
комнаты; критерии в `syncNoRoomClass` и `compactIcon` совпадают, мигания при `renderAll` нет;
второй копии правила нет; подсветка при перетаскивании и фактическая привязка не расходятся;
тай-брейк по `id` на гранях планарного графа недостижим (смежные комнаты делят ребро точно, и
точка на нём разрешается ветвью попадания).

**Не проверено:** браузером — ничего (у сабагента нет его инструментов). Учитывая, что сломанный
селектор оставляет 752 зелёными, DOM-часть правки — единственный непроверенный узел, и её
**надо посмотреть руками в браузере**.

## ПОРЯДОК РАБОТ ПО ВЕТКЕ

1. **Д1 — пересмотреть правило порядка ветвей.** Это не доводка, а замысел: нужен дискриминатор
   «противоречит ли grid-свидетельство допуску».
2. Покрыть метку поведенчески (инверсия критерия и слом селектора обязаны краснеть).
3. Д2, Д3, Д4, Д5 — мелкие, но Д3 про ложную денежную тревогу пользователю.
4. Проверить DOM-часть в браузере руками.
5. Только после этого — `ep-verify` и повторный состязательный, и лишь затем слияние.

### 2026-08-30 (ночь) — Ветка привязки: провал ревью исправлен, но ревью НЕ повторяли

**`wip/room-assign-tolerance`, HEAD `4eb486e`, 752 теста зелёные, запушена. В `main` пока НЕ вливать.**

Исправлено по находкам двойной проверки:
- **`ReferenceError` (`218c9d2`)** — `distancePointToSegment` не был объявлен в `app.js`
  (строка алиасов брала из `EPGeom` четыре функции без него, сама она выходит наружу только
  свойством namespace, а `app.js` — строгий IIFE). Исключение летело при ЛЮБОМ объекте вне
  контуров и роняло `renderAll`. Имя добавлено в ту же деструктуризацию, а не третьим стилем.
- **Порядок ветвей (`218c9d2`)** — было контур → grid → допуск, из-за чего допуск был мёртвым
  кодом: grid-совпадение это «та же компонента связности», а она через дверные проёмы накрывает
  всю квартиру, поэтому одна комната от инструмента «Комната» перехватывала все промахи.
  **Стало контур → допуск → grid.** Иерархия свидетельств: попадание бесспорно; допуск —
  «объект фактически на этом контуре», сильное; grid — «в той же компоненте, что и подпись»,
  слабейшее, и оно последнее. Проекты только с подписями не задеты (`polyRooms` пуст).
- **Класс «связка не исполняется» закрыт тестом** — он вырезает текст `resolveRoomForPoint`
  из `app.js` и исполняет в `vm`-контексте, куда кладёт РОВНО те имена, что `app.js`
  пробрасывает строкой алиасов. Забытый алиас снова даст `ReferenceError`, как в браузере.
  Прежние 743 теста этого не видели: поведенческий сам подставлял зависимость аргументом,
  структурный сверял текст регэкспом.
- **Два непокрытых инварианта закреплены** — попадание сильнее допуска; величина допуска
  пришпилена с обеих сторон.
- **Метка «вне помещений» (`4eb486e`)** — навешивалась только в `compactIcon`, то есть при
  создании иконки, а иконки пересоздаёт лишь `renderAll`. Восемь путей смены привязки его не
  зовут, включая правку вершин контура — тот самый сценарий, ради которого метка вводилась.
  Привязана к ЗАПИСИ `roomId` (обе функции, которые его пишут), а не к рендеру, поэтому у
  правки нет краёв. Плюс `.plan-icon:hover` гасил оранжевое кольцо при наведении — починено
  специфичностью.

## ЧТО ОСТАЛОСЬ ПО ВЕТКЕ — делать в следующей сессии

1. **ПОВТОРИТЬ ДВОЙНУЮ ПРОВЕРКУ.** Прошлая нашла 7 дефектов, включая неработающий код. После
   такого объёма правок прогонять `ep-verify` + `ep-adversary` заново **обязательно** — без
   этого в `main` не вливать. На остатке прошлой сессии запаса на это не было.
2. **Масштаб и детерминизм.** Допуск 12 px не учитывает `pxPerMeter` — на плане 22 px/м это
   55 см. Тай-брейк при равных расстояниях решает `String(id)`, а `id` — это `uid("room_")`,
   который **переприсваивается при каждом пересчёте помещений**: геометрия не менялась, а пост
   уехал из одной спальни в другую.
3. **Порядко-зависимая ветка в «едином правиле»** — `ctx.polyRooms.find(...)` при
   перекрывающихся контурах (достижимо правкой вершин) зависит от порядка `state.rooms`.
4. ~~Молчаливый пересчёт сохранённых проектов~~ — **снято**: живых проектов нет,
   см. правило в начале журнала. Не блокер, к владельцу не выносить.

### 2026-08-30 (поздно) — Ветка привязки ПРОВАЛИЛА двойную проверку. В `main` НЕ вливать

**`wip/room-assign-tolerance` (HEAD `2a2a34f`, 743 теста зелёные) НЕПРИГОДНА к слиянию.**
Обе проверки нашли по-крупному, `ep-verify` вынес «выполнение задачи НЕ подтверждаю».

**★ Правка физически не работает — `ReferenceError`.** `js/app.js:873` передаёт
`distancePointToSegment`, но это имя в `app.js` **нигде не объявлено**: строка алиасов
`js/app.js:512` деструктурирует из `EPGeom` без него, а `distancePointToSegment` живёт внутри
IIFE `geometry.js` и наружу выходит только как `EPGeom.distancePointToSegment`. `app.js` —
строгий IIFE, свободное имя даёт `ReferenceError`. **Проверил лично:** имя встречается в
`app.js` РОВНО ОДИН раз — на строке 873.
Последствие хуже, чем было: исключение летит при любом объекте, не попавшем ни в один контур,
и не поймано ни одним из 17 мест вызова — `renderAll`/`renderSummary` после
`recalculateRoomAssignments` просто не выполняются. Было «тихо неверные деньги», стало
«интерфейс встаёт».
**Почему 743 зелёных этого не видят:** поведенческий тест сам подставляет
`distancePointToSegment` из `geometry.js`, а структурный сверяет текст регэкспом — отсутствие
имени в области видимости ни один из них увидеть не может.

**Ещё 6 находок состязательного прохода:**
1. **Допуск недостижим, как только в проекте есть хоть одна ручная комната.** Порядок в
   `resolveRoomForPoint` — контур → grid → допуск, а grid-совпадение это «та же компонента
   связности», которая через дверные проёмы накрывает всю квартиру. Одна подпись-комната
   (инструмент «Комната» полигона не создаёт) перехватывает ВСЕ промахи, включая точку в 0 px
   от нужного контура. **Подтверждено мной вживую:** попытка воспроизвести `ReferenceError`
   через UI не сработала именно потому, что созданные инструментом комнаты бесполигонные и
   уводят в grid-ветку.
2. **Открытие сохранённого проекта молча меняет смету.** `recalculateRoomAssignments` затирает
   сохранённый `roomId` на первом же `renderAll` из `init()`; объекты, бывшие «без комнаты»,
   получают комнату задним числом — 40.52 € → 51.58 € на замере. Единственный индикатор
   (`orphanObjectsWarningHtml`) считает ОСТАВШИХСЯ сирот, то есть при подорожании двигается в
   сторону «стало лучше». Прецедент проекта прямо противоположный (`js/app.js:2721` —
   надбавка к курсу открывается с 0, «иначе сметы задним числом подорожали бы»).
3. **Метка «вне помещений» ставится в одном месте из десяти** — только в `compactIcon` внутри
   `renderAll`. Не появляется и не снимается в 8+ путях, включая `refreshRoomAfterEdit`
   (правка вершин контура), ради которого метка и вводилась. Строка предупреждения при этом
   утверждает «отмечены на плане» — в этот момент это ложь.
4. **Подтягивание из НЕразмеченного пространства** (коридор, улица) на 12 px без учёта
   `pxPerMeter`: на плане 22 px/м это 55 см. При равных расстояниях исход решает тай-брейк по
   `String(id)`, а `id` — это `uid("room_")`, который переприсваивается при каждом пересчёте
   помещений: геометрия не менялась, а пост уехал из одной спальни в другую.
5. **Два ключевых инварианта не покрыты тестами.** Мутации «допуск ДО попадания» и
   «`roomEdgeTolerance` 12 → 120» оставляют прогон зелёным. Это ровно те два свойства, вокруг
   которых написаны все комментарии правки.
6. **В «едином правиле» осталась порядко-зависимая ветка** `polyRooms.find` — при
   перекрывающихся контурах результат зависит от порядка `state.rooms`.

**Что проверки ПОДТВЕРДИЛИ:** денежная величина дефекта реальна (51.58 € против 40.52 € на
настоящем каталоге, 2146 товаров); допуск действительно не переопределяет настоящее попадание
(фаззинг 4000 точек × 40 перестановок, 0 нарушений); карта пространства строится один раз на
пересчёт, попавший в комнату объект не платит ни одного прохода по рёбрам; grid-комнаты не
сломаны; второй копии правила не осталось; притягивания через стену между двумя РАЗМЕЧЕННЫМИ
комнатами нет.

**Вывод: решение остановиться и не вливать было верным.** Ветка требует не доводки, а
пересмотра: помимо `ReferenceError` в ней два дефекта уровня замысла — порядок ветвей
(grid раньше допуска) и молчаливый пересчёт сохранённых проектов.



> Добавляй новую запись сверху. Формат: дата · что сделано · как сделано · проблемы.

> ### ⚠️ ИСПРАВЛЕНИЕ 30.08 — записям про «неработающий браузер» не верьте
>
> В записях за 28–30.08 несколько раз утверждается, что панель браузера в тех сессиях не
> работала и проверка в UI была невозможна. **Это неверно.** Браузер был запущен, приложение
> открыто на `localhost:8420`, и работали `navigate`, `read_page`, `get_page_text`,
> `read_console_messages` и выполнение JS в странице.
>
> Не работали ровно две вещи: **скриншоты** (панель в тот момент не отображалась —
> «not compositing frames») и **focus-события** (`document.hasFocus()` был `false`, поэтому
> Chrome подавлял `focus`/`blur`). Я обобщил этот частный отказ до «браузер не работает» и
> повторил вывод многократно, из-за чего проверка в UI не делалась там, где была доступна.
>
> **Что проверено вживую 30.08 (`main`, HEAD `af926a6`):** приложение грузится **без единой
> ошибки в консоли**; `EPRoomCarry`, `EPRoom`, `EPLightingByRoom` инициализированы; секция
> «Одиночные элементы» удалена (`#catalogSearch` отсутствует), библиотека постов на месте.
>
> **ПРОВЕРЕНО ВЖИВУЮ 30.08** (`main`, HEAD `370ff90`, приложение на `localhost:8420`):
> - консоль чистая, ошибок нет; `EPRoomCarry`/`EPRoom`/`EPLightingByRoom` инициализированы;
> - левая панель — три секции («Исходный план», «Инструменты», «Готовые посты»), секция
>   «Одиночные элементы» удалена (`#catalogSearch` отсутствует), каталог конструктора цел;
> - библиотека постов растянута: `min-height` 259 px (clamp сработал), список 234 px с
>   внутренней прокруткой — пустоты внизу панели нет;
> - **панель свойств комнаты**: селектор схемы на месте, опции — «Как в проекте (Классическая)»,
>   «Классическая», «Реле», «Звонковые кнопки — расчёт недоступен»;
> - подпись источника переключается «Унаследована от проекта: Классическая» ↔ «Своя схема
>   комнаты», значение переживает перерисовку панели;
> - **«Как в проекте» действительно УДАЛЯЕТ поле** — в снимке проекта в localStorage
>   `'lightingScheme' in room` даёт `false`. Проверено по сохранённым данным, не по виду.
>
> **Не проверено вживую:** обновление суммы при смене схемы комнаты (`bf8d744`) — нужен проект
> с постом и группой света, на пустом проекте сумма 0,00 € и до, и после. Логически правка
> верна: `#grandTotal` пишется только в `renderSummary`, и обработчик теперь его зовёт.
>
> **Урок для следующих сессий:** отказ одного инструмента браузера не означает, что недоступны
> остальные. Проверять поимённо. Утверждение «в UI проверить нельзя» требует такой же
> доказательности, как и любое другое.



### 2026-08-30 — Блок «схема электрики у комнаты» ЗАКРЫТ целиком

**731 тест зелёный.** Все три части плюс доводка по двойному ревью:
- **3.1** (`6b4bd47`) расчёт разрезан по комнатам, подпись кэша расширена `roomId` и схемами комнат;
- **доводка** (`6cbe081`) пять дефектов ревью: склейка пробелов в печати, сырой NUL-байт,
  разреженный список мест, `undefined` в местах, семантика `supported` (И → ИЛИ);
- **покрытие** (`73543ff`) семь пробелов слияния закрыты, тест детерминизма починен
  (его перестановки не нагружали сортировку), добавлен структурный тест проводки —
  тестовый хелпер оказался ВТОРОЙ КОПИЕЙ проводки из `app.js`;
- **3.2** документы: шапка больше не печатает схему проекта над механизмами чужой схемы
  («Схема: по комнатам» при разных); комната печатается у группы, реле и пробела; порядок
  комнат сведён к порядку листа монтажника («Без помещения» последним).

**Пиксельная привязка поста к комнате — РЕШЕНИЕ ПРИНЯТО, НАЧАТО И ОСТАНОВЛЕНО 30.08.**

> **Наработка лежит в ветке `wip/room-assign-tolerance` (коммит `b33df7d`), в `main` НЕ влита.**

> **ОБНОВЛЕНО 30.08:** на ветке появились **тесты чистого модуля** — 8 штук, 739 зелёных,
> мутационно проверены (снятие допуска, тай-брейк по порядку входа, комнаты без полигона),
> выживших мутантов нет; среди них регресс исходного замера `x=188`.
> **Чего ещё НЕТ:** покрытия связки `resolveRoomForPoint` в `app.js` (порядок «попадание → grid
> → допуск»; `app.js` в прогон не входит — нужен структурный тест по образцу
> `renderPropertiesStaleSelectionWiring`) и двойного прохода `ep-verify` + `ep-adversary`.
> Что я проверил ЛИЧНО: правило сведено в ОДНУ функцию `resolveRoomForPoint` (`js/app.js:866`),
> `getRoomForPoint` и `recalculateRoomAssignments` идут через неё — второй копии не осталось;
> карта пространства строится один раз на пересчёт; допуск `roomEdgeTolerance: 12` в `EPConfig`
> обоснован (центр иконки смещён на 12 px от якоря, и это ниже `snapRadius` 14).

> Там чистый модуль `js/roomAssign.js`, проводка в `app.js`, допуск в `config.js`, стиль пометки
> и подключение. **Тестов, мутационной проверки и двойного ревью там НЕТ.**
> Работу остановил осознанно: истекал пятичасовой лимит, правка денежная и требует прохода
> `ep-verify` + `ep-adversary`, на который запаса не оставалось. Класть половину денежной правки
> в `main` без проверки — ровно то, чего избегали: так уже вышло с `6b4bd47`, и состязательный
> проход потом нашёл в нём восемь краёв, включая расхождение экрана и документа на 15 €.
> Следующая сессия решает: продолжить с ветки или начать заново по постановке ниже.


**Решение владельца: допуск у границы + видимость.** Объект, не попавший ни в одну комнату,
но стоящий в пределах допуска от контура, привязывается к ближайшей; объект, оставшийся без
комнаты, виден всегда — на плане и в блоке групп света, а не только при перетаскивании.
Отвергнуты: ручное закрепление за комнатой (дороже, новое поле и орган управления) и
«только сделать видимым» (попадание по-прежнему решал бы один пиксель).

**ГЛАВНЫЙ КРАЙ ЭТОЙ ПРАВКИ (§7.1), установлен мной по коду.** Правило «в какой комнате точка»
написано в проекте ДВАЖДЫ:
- `getRoomForPoint(x,y,map)` — `js/app.js:836-850`, им пользуется подсветка цели при
  перетаскивании (`updateObjectRoom`);
- **отдельная копия внутри** `recalculateRoomAssignments` — `js/app.js:866-870`, это массовый
  пересчёт, который и проставляет `roomId`.

Допуск, добавленный в одну из них, даст особенно неприятный дефект: при перетаскивании
подсвечивается одна комната, а после отпускания пост уезжает в другую. **Правило надо свести
в одно место**, а не патчить обе копии.

Прочие требования постановки: допуск применяется ТОЛЬКО когда точка не попала никуда
(настоящее попадание не переопределять); математика — существующая
`EPGeom.distancePointToSegment` (`js/geometry.js:49`), второй реализации не заводить;
величина допуска — в `EPConfig` (`js/config.js`) рядом с `dragThreshold`/`roomAutoDelay`;
детерминизм при равном расстоянии до двух комнат обязателен (пост в проёме между кухней и
коридором равноудалён от обоих контуров); не превратить массовый пересчёт в построение
`buildSpaceComponents()` на каждый объект — сейчас карта строится один раз на всех, а сам
пересчёт идёт на каждый `renderAll()`.

Исходный замер, ради которого всё это: `x=187` → комната A, два переключателя, 51.58 €;
`x=188` → «без комнаты», два выключателя, 40.52 €. **Один пиксель = 11.06 € и другая схема
проводки.** Выключатель ставят у дверного проёма, то есть ровно на границе контура, а точка
на границе считается снаружи.

`recalculateRoomAssignments` считает `pointInPolygon(obj.x+12, obj.y+12, …)` на каждый
`renderAll()`, и теперь это решает деньги: замер `x=187` → комната A, два переключателя,
51.58 €; `x=188` → «без комнаты», два выключателя, 40.52 €. **Один пиксель = 11.06 € и другая
проводка.** Выключатель ставят у дверного проёма, то есть ровно на границе контура, а точка на
границе считается снаружи. Нигде не видно, что пост выпал из комнаты. Владелец знает, решено
делать после документов.

### 2026-08-30 — Независимый контроль: суть подтверждена, найден регресс печати

`ep-verify` вернулся (в отличие от прошлой записи — успел). Проверял ровно то, что попало
в `6b4bd47`: сверил md5 файлов байт в байт, заметив, что коммит случился во время его работы.

**ПОДТВЕРЖДЕНО на фактах, не по отчёту:**
- 694 → 706 тестов; базовую линию снял независимо, отдельным `git worktree` на `50768c0`.
- **Деньги — на НАСТОЯЩЕМ каталоге** (2146 товаров), не на заглушке: «Кухня» с двумя местами
  в комнате A и одним в B: было 93.91 € (2 переключателя + инвертор), стало 71.84 €
  (2 переключателя + выключатель). Тот же результат через документный путь
  (`rowsByPost` + `EPEstimate.lightingCounts`). Сходится до копейки.
- **Контракт модуля дословно:** одно место, поданное трижды, даёт 93.91 € (позиционность цела);
  `"4.10"` и `"4.1"` — две разные группы; регистр схлопывается; серии не приводятся.
- **Слияние индексов сверено ДИФФЕРЕНЦИАЛЬНО** — своя реализация партиционирования против
  правкиной, **180 случайных проектов, расхождений нет**. Дубль в не первой партиции даёт
  глобальный индекс, а не индекс подсписка.
- **Обратная совместимость на данных:** 200 проектов в одной комнате и 300 в трёх комнатах без
  своих схем — деньги и роли идентичны прежнему расчёту (97 183.50 € суммарно). Разошлись
  ровно те 300, где есть межкомнатные одноимённые группы, и только по этой причине.
- **Детерминизм: 2160 прогонов** (720 перестановок постов × 3 порядка комнат) — расхождений 0.
- **Кэш** сбрасывается на смену схемы комнаты и на переезд поста; без сброса отдал бы
  51.58 € вместо 40.52 €.
- Тавтологий в тестах нет: гоняют настоящие `LG.plan` и `Room.roomLightingScheme`.

**НОВАЯ НАХОДКА, которой не увидел состязательный — регресс печатного блока (Р5).**
`planByRooms` конкатенирует `gaps` подпланов, а `addGap` внутри одиночного `plan` склеивал
одинаковые `kind+groupKey`. Итог: три места без группы в трёх комнатах печатают ТРИ одинаковые
строки «Не указана группа света · мест: 1» вместо одной «мест: 3». То же с
`relay-article-unknown` — по строке на каждую relay-комнату. Деньги не двигает, но блок
«Группы света» в КП размножает одинаковые предупреждения. **Не исправлено.**

**Расхождения с отчётом исполнителя:**
- мутация «партиционирование» даёт **5** красных тестов, а не 6, как он заявил;
- **проводка в `app.js` не покрыта ничем**, и хуже: тестовый `planByRoomsFor`
  (`tests/lightingByRoom.test.js:38-51`) — это **вторая копия** проводки app.js. Ошибка в
  `partitionKeyOf`/`schemeForPartition`/`roomOfPost`/вызове `cacheSignature` останется зелёной.
  Прецедент структурного теста по тексту есть — `tests/renderPropertiesStaleSelectionWiring.test.js`;
- `tests/supplierSpec.test.js:387` утверждает «расчёт ровно так, как подставляет app.js
  (lightingFor)» — устарело, инвариант «свод = смета» прогоняется только по одиночному пути.

**Пробелы покрытия — 15 мутаций слияния тесты НЕ ловят** (обе проверки сошлись независимо).
Для каждой доказано, что выдача реально меняется: пропажа всех `gaps`; неперемапленные
`order`/`groups[].places`/`gaps[].places`/`unassigned.places`/`place.index`; непереносимые
`relays` (а они печатаются в КП); несложенные `relayTotal`/`totalsRequired`/`missingTotal`;
`supported` всегда true; слияние ключа «нет комнаты» с комнатой `id=null`; убранная сортировка
партиций — **144 из 180 проектов расходятся, 1980 из 2160 перестановок дают другой порядок
печати**. Существующий тест детерминизма поймать это НЕ СПОСОБЕН по построению: обе его
перестановки оставляют комнату A первой по появлению. Нужна перестановка, где первой во входе
идёт комната B.

**Что `ep-verify` не проверил:** UI в браузере (панель неработоспособна), полную сборку КП
через `offerPdf`.

### 2026-08-30 — Состязательный проход по расчёту: 8 находок, одна починена

**Состояние: 706 тестов зелёные, закоммичено и запушено.**

`ep-adversary` разобрал `6b4bd47` (по-комнатный расчёт) и нашёл **8 краёв**.
`ep-verify` не успел вернуться до конца окна — **независимый контроль по этой правке
по-прежнему НЕ проведён**.

**ПОЧИНЕНО (главная находка).** Смена схемы у комнаты меняла деньги, но не обновляла их
на экране: обработчик `roomSchemeSelect` звал только `renderProperties()`+`persistProject()`,
а `#grandTotal`/`#specList`/`#lightingSummary` пишутся ТОЛЬКО в `renderSummary()`
(`js/app.js:1530`, единственная запись). Кэш при этом сбрасывался правильно — значит панель
показывала старую сумму, а следующий экспорт КП печатал новую: **экран и документ об одном
проекте противоречили друг другу**. Замер агента: 71.84 € на экране против 56.26 € в документе.
Классический край §7.1 — комментарий в коде ещё говорил «расчёт здесь не трогаем (часть 3)»,
часть 3 приехала, а обработчик остался. Починено списком потребителей как у схемы ПРОЕКТА.
*Правку сделал основной агент лично, а не сабагент — окно лимита истекало.*

## НЕ ПОЧИНЕНО — разобрать в следующей сессии

**Требуют решения ВЛАДЕЛЬЦА:**

> **РЕШЕНО 30.08 (владелец):** по первому пункту — миграция и предупреждение НЕ нужны,
> «сейчас всё тестовое, нам не страшно». **Условие решения: реальных проектов ещё нет.**
> Оно перестаёт действовать, как только у заказчика появятся живые проекты или уйдут
> сметы клиенту — тогда молчаливый пересчёт при открытии придётся закрывать заново.
> Второй пункт (пиксельная привязка) этим решением НЕ закрыт: он про текущее поведение,
> а не про старые данные, и на живых проектах проявится так же.

- **Старые проекты молча пересчитываются при открытии.** Группа «Кухня» по одному месту в трёх
  комнатах: было 93.91 € (два переключателя + инвертор), стало 60.78 € (три выключателя) —
  **−33.13 € без единого слова**, в том числе в уже отправленных клиенту сметах. Разделение
  групп владелец утвердил, но «задним числом дешевеют ранее выставленные сметы» — отдельное
  решение. В том же `restoreProject` есть прямой прецедент ПРОТИВОПОЛОЖНОГО выбора: старый
  проект без `rateSurchargePercent` открывается с 0, а не с дефолтной 3, «иначе сметы задним
  числом подорожали бы, и этого никто бы не заметил».
- **Привязка поста к комнате пиксельная и теперь решает деньги.** `recalculateRoomAssignments`
  считает `pointInPolygon(obj.x+12, obj.y+12, …)` на каждый `renderAll()`. Замер: `p2.x=187` →
  комната A, два переключателя, 51.58 €; `p2.x=188` → «без комнаты», два выключателя, 40.52 €.
  **Один пиксель = 11.06 € и другая проводка.** Выключатель ставят у дверного проёма, то есть
  ровно на границе контура, а `pointInPolygon` границу трактует как «снаружи». Нигде не видно,
  что пост выпал из комнаты.

**Технические, решения не требуют:**
- Заголовок блока в документе печатает схему ПРОЕКТА над механизмами, подобранными по схеме
  КОМНАТЫ: «Схема: Звонковые кнопки» над выключателями, «Схема: Классическая» над импульсным
  реле. (Это часть 3 пункт 2 — документы.)
- Одноимённые группы разных комнат в документе неразличимы: две одинаковые строки «Группа
  "Кухня" · мест управления: 1». Порядок групп («без комнаты» ПЕРВОЙ, комнаты по строковому id)
  противоречит порядку в листе монтажника («Без помещения» ПОСЛЕДНИМ).
- Разреженный список мест (`delete places[i]`) теряет место: `forEach` в дыры не заходит.
  Соседний `lightingGroups.js:393-400` этот случай держит НАМЕРЕННО и описывает как реальный.
- `undefined` в списке мест роняет весь расчёт (`TypeError` из `projectLighting` → `renderSummary`)
  вместо честного пробела, как делает одиночный `plan`.
- Одна комната на «Звонковых кнопках» ставит `supported:false` ВСЕМУ проекту (латентно —
  наружу `plan.supported` сегодня никто не читает).
- `js/lightingByRoom.js:42` — часовой партиции записан **сырым NUL-байтом** вместо `\u0000`:
  git и grep считают модуль бинарным, дифф нечитаем.

> **ЗАКРЫТО 30.08** (`6cbe081` — пять дефектов, следом тесты, 706 → 722). Перемер после
> доводки показал: из пятнадцати мутаций семь всё ещё проходили зелёными — на них написаны
> поведенческие тесты. Тест детерминизма починен (его перестановки не нагружали сортировку
> партиций). Добавлен структурный тест проводки: тестовый хелпер `planByRoomsFor` оказался
> ВТОРОЙ КОПИЕЙ проводки из `app.js`, то есть тесты проверяли копию, а не оригинал.
> Пять дефектов доводки: склейка пробелов в печати, сырой NUL-байт, разреженный список мест,
> `undefined` в местах, семантика `supported` (И → ИЛИ, план больше не лжёт о себе).

## ПРОБЕЛЫ ПОКРЫТИЯ — тесты не держат правку

706/706 остаются зелёными при этих мутациях (проверено прогоном):
- **пять из шести перемапов индексов** по отдельности (`places[].index`, `order`,
  `groups[].places`, `unassigned.places`, `duplicates`, `gaps[].places`) — краснеет только
  общий `remap = j => j`, и то лишь потому, что ломает выравнивание `places`;
- **сортировка партиций** — а она реально нужна: без неё вход `[a,b]` даёт «Зал, Кухня»,
  вход `[b,a]` — «Кухня, Зал». Существующий тест «результат не зависит от порядка» её не ловит:
  обе его перестановки дают один и тот же порядок первого появления комнат;
- **перенос `relays` в слитый план** — а он печатается в КП строками «Импульсное реле · группа…»,
  то есть его можно потерять целиком незаметно;
- `supported`, `totalsRequired`, `missingTotal`.

Что агент проверил и дефекта НЕ нашёл: перемап всех шести полей в коде на месте; ни один
потребитель не индексирует `plan.groups` по ключу (перебраны все документы, смета, свод,
конструктор, панель свойств); каждое место попадает ровно в одну партицию; старого пути
«одной схемой на проект» не осталось; подпись кэша перебрана по всем видам изменений;
расчёта по устаревшей привязке нет; схема комнаты переживает перетрассировку.

### 2026-08-30 (ночь) — Блок «схема электрики у комнаты»: части 1-2 готовы, часть 3.1 без проверки

**Состояние: 706 тестов зелёные, дерево чистое, всё закоммичено и запушено.**

Коммиты: `8df96dd` → `2fd8b8b` → `50768c0` → `6b4bd47`.

**Часть 1 (`8df96dd`) — пересчёт контуров больше не теряет имя и площадь комнаты.**
`detectRooms`/`detectRoomsML`/`buildRoomsFromLines` выбрасывают авто-комнаты и создают заново
с новым `uid`; ручную правку КОНТУРА это переживает (`markRoomEdited` снимает `autoPolygon`),
а набранные руками имя и площадь — нет. Сопоставление старых и новых комнат вынесено в чистый
`js/roomCarry.js`: двунаправленное попадание центроидов + порог по отношению площадей 0.25,
назначение один-к-одному, детерминированный тай-брейк по геометрическим ключам. Автоимена
«Комната N»/«Помещение N» не переносятся — конфликтуют с нумерацией.

**Краш (`2fd8b8b`).** `renderProperties` падала `TypeError`, когда `state.selected` указывал на
исчезнувший объект. Оказалось ОДНО правило, дырявое во ВСЕХ четырёх ветках (не только комната).
Починено одной проверкой до входа в ветки + единый резолвер `findSelectedEntity`.

**Часть 2 (`50768c0`) — схема стала свойством комнаты.** `EPRoom.roomLightingScheme` в новом
`js/room.js`: отсутствие поля/мусор/неизвестный id → схема проекта. Селектор в панели свойств
комнаты; пункт «Как в проекте» **удаляет поле**, а не пишет значение проекта. Схема переносится
в `roomCarry` наравне с именем и площадью — иначе стиралась бы при каждой правке линий разметки.

**Часть 3, пункт 1 (`6b4bd47`) — ДЕНЕЖНАЯ ПРАВКА, НЕ ПРОВЕРЕНА.**
Расчёт разрезан по комнатам, подпись кэша `projectLighting` расширена `roomId` постов и схемами
комнат. Утверждённое владельцем изменение поведения: **одноимённые группы в разных комнатах
больше не схлопываются** — суммы в существующих проектах изменятся.

---

## ГДЕ ОСТАНОВИЛИСЬ — читать первым делом

**1. `6b4bd47` не прошёл двойную проверку.** `ep-verify` и `ep-adversary` были запущены
параллельно и не успели вернуться до конца сессии — их отчётов НЕТ. Коммит сделан, чтобы работа
не пропала, а не как признак готовности. **Проверку надо запустить заново.**
Что им ставилось искать (самые вероятные места молчаливой порчи денег):
- перемап индексов при слиянии подпланов — поле, которое забыли перемапить;
- **дубли ключей в слитом плане**: две комнаты дают две группы «Кухня» с одинаковым ключом.
  Я проверил лично единственного потребителя — `buildHtml` (`js/lightingPlan.js:246`) перебирает
  список, а не индексирует по ключу. Но будущий потребитель, построивший `Map` по
  `plan.groups[].key`, схлопнет их обратно и потеряет деньги;
- обратная совместимость: проекты без комнат, посты без `roomId`, `roomId` на исчезнувшую комнату;
- нестабильность `roomId` — он пересчитывается на каждый `renderAll()`;
- изменение, меняющее результат, но НЕ меняющее подпись кэша.

**2. Известный дефект, найден мной, НЕ исправлен.** `js/lightingByRoom.js:42` — часовой партиции
«без комнаты» записан **сырым NUL-байтом** вместо escape `\u0000`:
`const partitionNorm = key => (key == null ? "<NUL>" : "r:" + String(k…`.
Функционально работает (NUL в строковом литерале валиден), но `git diff` и `grep` считают файл
**бинарным** — дифф по модулю нечитаем, ревью правок в нём невозможно. Приём к тому же избыточен:
ключи комнат уже namespace-нуты префиксом `"r:"`, часовому достаточно быть любой строкой без
этого префикса. Не чинил на месте, потому что оба ревьюера в тот момент читали файл и делали
на нём мутационные прогоны.

**3. Осталось в части 3:** пункт 2 — провести схему в документы (смета, КП, лист монтажника,
свод). Сейчас заголовок блока «Схема» при смешанных схемах показывает схему ПРОЕКТА, а не
«по комнатам», и пробелы вида «схема не описана» могут повторяться по одному на комнату.

**4. Ничего из сделанного 28-30.08 не проверено в браузере.** Панель браузера в этих сессиях не
работает: скриншоты недоступны, `document.hasFocus()` всегда `false`, focus-события подавлены.
Не смотрели глазами: черновик комнаты, селектор типа стены поста и в конструкторе, раскладку
левой панели после удаления одиночных элементов, бейдж карточки, селектор схемы у комнаты.
**Это первое, что стоит сделать при живом браузере.**

### 2026-08-29 (ночь) — Тип стены у поста, библиотека, чистка. Коммиты `058dbb5`…`4ad9245`

**Состояние: 667 тестов зелёные, дерево чистое, 6 коммитов в `main`, НЕ ЗАПУШЕНО.**

Коммиты: `b7953ab` (черновик комнаты) → `29b530a` (журнал) → `714ad92` (правило §7.2)
→ `058dbb5` (тип стены + одиночные элементы) → `7aad7df` (библиотека постов) → `4ad9245` (README).

**Тип стены — оказался уже наполовину сделанным.** Владелец просил «для каждого поста
сохранять выбор варианта стен», но `post.wallType` и `EPPosts.postWallType` существуют
с `a74033a` (фикс B5). Не хватало только органа управления. Сделано:
- селектор в панели свойств поста с подписью, откуда значение: «Свой тип стены поста»
  либо «Унаследован от проекта». Это РАЗНЫЕ состояния — отсутствие поля значит «следовать
  проекту», а не «неизвестно». Выбор уже действующего значения намеренно ничего не пишет,
  иначе человек, ткнув в тот же пункт, молча превратил бы унаследованный тип в собственный;
- кнопки в конструкторе разблокированы для нового поста и шаблона; **шаблон теперь несёт
  `wallType`**, `placementFields` переносит его в пост. Поле пишется только как явное
  расхождение с проектом — иначе текущее значение проекта запеклось бы в каждый новый шаблон.
  Старые шаблоны без поля продолжают читать проект: `placementFields` НЕ создаёт ключ при
  отсутствии или мусоре.
- Запрет из B5 в силе: `EP_DATA.settings.wallType` присваивается ровно в одном месте —
  в панели «Спецификация» (`js/app.js:3330`), проверено грепом.

**Одиночные элементы убраны из левой панели** (решение заказчика 24.08, §4.8 «элементы только
в блоках»). Убрана возможность ДОБАВЛЯТЬ; обработка уже сохранённых не тронута — из заказа их
выкидывать нельзя (`js/supplierSpec.js:182`). Каталог конструктора (`renderBuilderCatalog`) —
другая функция, не задет.

**Библиотека постов.** Высота секции: flex не помогал, потому что у `.sidebar` `overflow:auto`
и верхние секции съедают всю высоту — свободного места для `flex:1` нет. Поменян пол сжатия:
`clamp(220px,36vh,420px)`. Бейдж карточки показывал число механизмов («2 места» рядом с
«Пост на 3 модуля» читалось как счётчик размещений) — теперь занятость модулей рамки тем же
расчётом, что в конструкторе.

**Разобран вопрос владельца «изменённый пост числится как старый».** Не подтвердился:
`templateId` у размещённого поста записывается, но **не читается ни одной строкой** во всём
`js/`. Смета, КП и лист монтажника группируют по фактическому составу (накладка + мультимножество
механизмов), а не по происхождению. Вводил в заблуждение только бейдж.

---

## ГДЕ ОСТАНОВИЛИСЬ — читать это первым делом

Идёт блок **«схема электрики у комнаты»**. Решения владельца приняты:
- схема хранится **у комнаты**, проект — значение по умолчанию, **плюс защита от стирания**;
- группы с одинаковым именем в разных комнатах **разделяются по комнатам** (сегодня схлопываются).

Блок разбит на три части, коммитить каждую отдельно:

**Часть 1 — перенос пользовательских полей при пересчёте контуров. БЫЛА В РАБОТЕ, НЕ ЗАВЕРШЕНА.**
Агент оборвался на лимите, на диск ничего не попало — начинать заново.
Суть: `detectRooms` (`js/app.js:533`), `detectRoomsML` (`:561`) и `buildRoomsFromLines` (`:2338`,
зовётся автоматически через 600 мс из `scheduleRoomsFromLines`) делают
`state.rooms=state.rooms.filter(r=>!r.autoPolygon)` и создают комнаты заново с новым `uid`.
Всё, что человек ввёл руками, теряется — **сегодня это уже происходит с именами комнат**.
Нужна чистая функция сопоставления старых и новых комнат по геометрии (новый `js/roomCarry.js`
по образцу `js/roomDraft.js`), переносящая имя и площадь. Требования: автоимена
(«Комната N», «Помещение N») НЕ переносить — конфликтуют с нумерацией; сопоставление
детерминированное (у проекта история дефектов «зависит от порядка входа»); один к одному;
явный порог совпадения; продумать разделение и слияние комнат; не падать на комнате без
полигона (`js/app.js:3245`).

**Часть 2** — поле схемы у комнаты и орган управления в панели свойств комнаты.

**Часть 3 (самая денежная)** — по-комнатный расчёт. `lightingFor` (`js/app.js:1272-1294`)
сегодня считает весь проект ОДНИМ вызовом `EPLightingGroups.plan({scheme:<одна строка>,
places:<все места проекта>})`. Подпись кэша `projectLighting` (`:1345-1351`) **не содержит
`roomId`** — без её расширения смена схемы у одной комнаты не сбросит кэш (этот класс дефекта
в модуле уже ловили). Проверить ключи группировки сметы (`js/estimate.js:247-266`), провести
схему в КП, лист монтажника и свод. `roomId` в `lightingGroups.js`/`lightingPlan.js` сегодня
не упоминается ни разу.

**Мелочи, подобрать между частями:**
- **Предсуществующий краш:** пересчёт уничтожает выделенную комнату, `state.selected` никто не
  чистит, `renderProperties` падает `TypeError` на `getObjectsInRoom(r.id)` (`js/app.js:1191`).
  Воспроизводится и без правок этой сессии.

**Ограничение среды (важно):** панель браузера в этих сессиях не работает — скриншоты
недоступны, `document.hasFocus()` всегда `false`, focus-события подавлены. **Ничего из
сделанного 28–29.08 не проверено глазами**: ни черновик комнаты, ни селектор типа стены,
ни новая раскладка панели. Всё держится на тестах и чтении кода. Визуальную проверку
нужно сделать при первой возможности.

### 2026-08-28 (день) — Черновик комнаты в панели свойств. Закоммичено `b7953ab`

**Состояние: 663 теста зелёные (было 647), закоммичено в `main`, НЕ запушено.**

**Закрыта продуктовая развилка** из прошлой записи — потеря несохранённого черновика
имени/площади комнаты. Решение владельца: сбрасывать черновик перед перерисовкой.

**Формулировка развилки была неточна — это выяснилось разведкой и меняет суть.**
«Черновик живёт до кнопки "Сохранить изменения"» неверно: `saveRoom` висит на ТРЁХ
событиях — кнопка, `Enter` и `blur`, причём `onblur=saveRoom` существует **с первого
коммита** (`git log -S 'onblur=saveRoom'` → только `b27a9b9`). Отсюда следствие: смена
настройки проекта черновик потерять НЕ может — чтобы тронуть настройку, надо
сфокусировать другой орган, а это снимает фокус и коммитит. Реальное окно потери у́же:
перерисовка приходит, пока поле в фокусе, то есть только по асинхронным путям —
ответ курса ЦБ (`refreshRate`→`applyProjectSettings`), таймер 600 мс авторасчёта комнат,
автотрассировка. **Не наследуйте прежнюю посылку из записи за 28.08 (ночь).**

**Как сделано.** Правило в одной точке: `flushRoomDraft()` первой строкой
`renderProperties()`, до раннего `return` по `!state.selected` — оттуда покрывает все
~25 вызовов, краёв по потребителям нет (§7.1 п.2). Решение «коммитить ли и что именно» —
в чистом модуле `js/roomDraft.js` (`EPRoomDraft.commit`), без DOM и state, поэтому
проверяется автотестом. Коммит идемпотентен: значения комнаты нормализуются теми же
правилами, что и ввод, поэтому холостая перерисовка ничего не тянет.

**Найденная и исправленная регрессия (её нашёл состязательный проход, не автор и не
независимый контроль).** Первая версия звала `renderRooms()` изнутри `flushRoomDraft`.
Но `renderProperties()` вызывается из `beginPress` в начале жеста перетаскивания, а
`renderRooms()` первой строкой сносит все `.room-label` — вместе с узлом, на котором жест
только начинается. Следствия: `setPointerCapture` уходил в отсоединённый узел (исключение
глотал `try{}catch(_){}`), перенос комнаты не работал, `onUp` не приходил, и повешенный на
`document` capture-обработчик Esc **не снимался никогда** — Escape глох во всём приложении
до перезагрузки. Правка нарушала инвариант, записанный в коде явным текстом:
«Сцена на нажатии НЕ перерисовывается (корневой дефект)» (`makeDraggable`).
Починено точечным обновлением одного узла подписи (`updateRoomLabelText`) по образцу
`applySelectionClasses` — полной переигровки слоя больше нет.

**Что ещё вскрылось:**
- Ложный коммит на комнате без полей `name`/`area` (посторонний localStorage):
  `"" !== undefined` давал `commit:true` и лишний `persistProject`. Закрыто нормализацией
  полей комнаты теми же правилами, что и ввода.
- Шапка `roomDraft.js` заявляла покрытие пути авторасчёта комнат — **неверно**:
  `buildRoomsFromLines`/`detectRooms`/`detectRoomsML` делают
  `state.rooms.filter(r=>!r.autoPolygon)` и создают комнаты заново с новым `uid`, черновик
  там теряется молча и покрыть это нельзя — объект уничтожается по замыслу пересчёта.
  Комментарий приведён в соответствие с фактом.
- **Предсуществующий краш, НЕ чинили:** когда пересчёт уничтожает выделенную комнату,
  `state.selected` никто не чистит, и `renderProperties` падает `TypeError` на
  `getObjectsInRoom(r.id)`. Воспроизводится и без нашей правки. Отдельная задача.

**Про тесты — важный урок.** `js/app.js` не грузится НИ ОДНИМ тестом, поэтому проводка
(сам факт вызова и его позиция) не держалась ничем: удаление строки `flushRoomDraft();`
оставляло все тесты зелёными. Заведён структурный тест по исходному тексту
(`tests/roomDraftFlushWiring.test.js`) — разбором с регэкспами, устойчивым к
форматированию. Мутацию проверил лично: удаление вызова роняет тест.

**Ограничение среды, важное для следующих сессий.** Панель браузера в этой сессии не
отображалась: скриншоты не работают, `document.hasFocus()` всегда `false`, focus-события
подавлены. **Живое воспроизведение потери черновика и поведения фокуса невозможно.**
Первый замер я сделал до того, как это выяснил, и он оказался невалиден. Утверждение
«удаление сфокусированного инпута через `innerHTML` не рождает `blur`» опирается на
спецификацию и на прогон вербатим-срезов кода в node на DOM-шиме, а не на браузер.
**Осталось проверить вживую:** сохраняется ли набранное при асинхронной перерисовке,
и что происходит с фокусом и кареткой (значение сохраняется, но каретка из поля выпадает).

**Организационное.** В начале сессии в этом же дереве работали три другие сессии Claude;
одна переключила ветку на `docs/clean-readme-artifact` и закоммитила туда чистку
`README.md`. По решению владельца ветка выброшена (`git branch -D`). **Побочный факт:
в `main` остаётся битый хвост `README.md`** — 30 байт строки `# electroPlan`, записанной
в UTF-16LE и приклеенной к UTF-8 файлу. Тот коммит чинил ровно это; чистка не сделана.

### 2026-08-28 — Корневой CLAUDE.md и три недостающих сабагента под workflow §7.2

**Что сделано:** правила §7.1/§7.2 описывали роли разведки, независимого контроля
и состязательной проверки словами, но сабагентов под них не было — `ep-dev` закрывает
только реализацию, а `bitrix-reviewer`/`project-mapper` заточены под Bitrix-скелет
в `/local/` и не покрывают `js/*.js`/`index.html`, на котором сейчас идёт вся работа.
Плюс отсутствовал `../CLAUDE.md` (корень `electro/`), на который ссылается шапка
этого файла (правило «читать HANDOFF.md первым делом»).

**Как сделано:**
- `electro/CLAUDE.md` — короткий корневой файл: что за каталог, ссылка на обязательное
  чтение `electroplan-project/HANDOFF.md`, правило отвечать по-русски.
- `.claude/agents/ep-recon.md` — разведка (read-only) по `js/*.js`/`index.html`/`css/`/`tools/*`:
  где что лежит, точки встраивания, риски, похожие решения. Вызывается до `ep-dev`.
- `.claude/agents/ep-verify.md` — независимый контроль: не доверяет отчёту исполнителя,
  сам гоняет `npm test`/`node --check`/логику на реальных данных.
- `.claude/agents/ep-adversary.md` — состязательный ревью по правилам §7.1: ищет края
  правки (те же типовые классы — не во всех похожих местах, забытая обратная
  совместимость, дублирующий путь мимо вынесенной функции), не чинит и не подтверждает
  без доказательств.

**Проверка:** файлы созданы, синтаксис фронтматтера (`name`/`description`/`tools`/`model`)
проверен по образцу существующих `ep-dev.md`/`bitrix-reviewer.md`/`project-mapper.md`.
Сабагенты вживую не вызывались — первый реальный вызов будет проверкой формата отчёта.

**Не сделано:** решение по коммиту оставлено владельцу — сессия работает по общему
правилу «коммитит и пушит основной агент» (§7.2), но здесь коммит явно не запрошен,
поэтому файлы лежат в рабочем дереве.

### 2026-08-28 (ночь) — Конструктор, группы света в приложении, оба бага с записи встречи

**Состояние: 647 тестов зелёные (было 281 в начале работы), всё закоммичено и запушено в `main`.
Ветка `client` не тронута — заказчик по-прежнему на версии от 01.08.**

Коммиты ночи: `0173b28` → `a803f09` → `ff65f76` → `ae79252` → `9371167` → `a74033a`.

**Сделано:**
- **Модуль групп света** доведён до готовности (6 раундов ревью) и **подключён к приложению**:
  схема электрики выбирается на проекте, у клавиши указывается группа, механизм подставляется
  расчётом и попадает в смету, КП, лист монтажника и свод. Проверено вживую: «Кухня» и «кухня»
  схлопываются в одну группу и дают два переключателя, «4.10» остаётся отдельной.
- **Полноэкранный конструктор поста** (просьба владельца): крупные карточки с фото, разделы
  из «Функциональной группы» номенклатуры свёрнуты по умолчанию, поиск по артикулу.
- **Сводная спецификация для поставщика** — отдельной страницей в КП и листе монтажника.
- **B4 и B5** — оба бага с записи встречи, причём **обе гипотезы о причинах оказались неверны**
  (см. ниже).

**Найдено и починено попутно — никто из этого не искал:**
- смета **теряла позиции молча**: механизм не из каталога исчезал из состава поста вместе
  с ценой и без следа в списке пропусков;
- цена поста считалась **двумя формулами** — 77.86 € в панели и 103.65 € в смете за один пост;
- поломка одного поста **меняла механизмы в соседних** (проходная схема физически ломалась);
- кнопка «Перенумеровать посты» **молча меняла сумму** на 25 €;
- курс ЦБ пересчитывал каталог в рубли, а **цена в карточке оставалась в евро**;
- **высота элемента не сохранялась** — правка терялась при перезагрузке;
- колонка `«Подгруппы »` не читалась **никогда** с начала проекта (`null` у всех 2146 позиций).

**Про B4 и B5 — важный урок, он же пример к §7.1 и §7.2.**
Я предполагал: B4 — конфликт двойного клика с перетаскиванием; B5 — свойства читаются
из шаблона через `templateId`. **Обе гипотезы неверны.**
- B4: жест исправен. Не сбрасывалось значение `<select>` накладки между открытиями окна, и при
  втором открытии чужая накладка побеждала родную. Тихий вариант опаснее того, на что жаловался
  заказчик: при совместимой чужой накладке подмены не видно, и пост за 68.07 € молча сохранялся
  с «Крышкой IP55» за 20.71 €.
- B5: `templateId` в проекте **нигде не читается**. Тип стены был глобальной настройкой ПРОЕКТА,
  а кнопки стояли в окне поста — менялся весь проект, включая посты другой серии.
Если бы агент отрабатывал мои гипотезы, он «починил» бы несуществующее. Спасло указание
«сначала воспроизведи, гипотезы проверь и отвергни, не изобретай починку несуществующего».

**Принципы, выведенные по ходу и закреплённые в коде:**
1. *Настройка проекта правится только в панели проекта*; окно объекта её показывает, но не меняет.
   Селектор схемы электрики убран из конструктора: там каждый орган — черновик, откатываемый
   «Отменой», а схема действовала мгновенно и необратимо.
2. *Одна точка «настройка проекта изменилась»* — `applyProjectSettings()` вместо списка вызовов
   в каждом обработчике. Новый обработчик не обязан помнить потребителей.
3. *Подтверждение опасной команды — другим ЖЕСТОМ, а не паузой.* Воспроизведено, что любая
   граница по времени поток только задерживает: клики раз в 850 мс применяли команду со второго.
4. *Миграция чинит уже сохранённые данные*, а не только новые — иначе фантомные места остаются
   в старых проектах и оживают после перезаливки прайса.

**Открытая продуктовая развилка для владельца:** ветка комнаты в панели свойств держит
несохранённый черновик (имя/площадь) до кнопки «Сохранить изменения», а панель теперь
перерисовывается на любой смене настройки проекта — черновик теряется чаще, чем раньше.
Либо сохранять по вводу (как теперь у высоты элемента), либо научить перерисовку беречь
незавершённый ввод. Решение за владельцем.

**Не начато:** D10 выбор столбцов в документах, E13 фиксация коллекции за помещением,
E14 фильтр по цвету, E16 стандарт первым шагом, E18 аксессуары (блокирован ответом заказчика),
E19 только посты на плане, D12 взрыв-схема на выбор.
**Ждёт владельца:** ветка `client` (заказчик не видел ничего из сделанного) и письмо
с 9 вопросами, четыре блокирующих.

### 2026-08-27 (ночь) — Группы света, сводная спецификация, два бага сметы. Закоммичено `0173b28`

**Состояние: 489 тестов зелёные, всё закоммичено и запушено в `main`. Ветка `client` не тронута.**

**1. `js/lightingGroups.js` — расчёт механизмов по схеме электрики (ядро готово).**
Классическая: 1 место → `ХХ001.0`, 2 → два `ХХ005.0`, N≥3 → два `ХХ005.0` + (N−2) `ХХ013.0`.
Реле: `ХХ008.0` на месте + реле на группу (до 4 кнопок). Отдаёт распределение ПО МЕСТАМ,
а не сумму — механизмы стоят в разных постах.
*Не реализовано намеренно:* «звонковые кнопки» (схема нигде не описана) и артикул реле
(`03992` нет ни в номенклатуре, ни в прайсе; у заказчика в накладной стоит стороннее реле Finder).
**Модуль пока не подключён к `app.js` — интеграция идёт отдельной задачей (C8).**

**2. `js/supplierSpec.js` — сводная спецификация по артикулам для поставщика.**
Отдельной страницей в КП и листе монтажника, колонки как в накладной заказчика.

**3. Починены два бага в смете**, вскрытых инвариантом «два документа об одном проекте
не могут противоречить»:
- механизм или накладка, которых нет в каталоге, **исчезали из состава поста молча**
  (`product(id) && product(id).name` → `undefined` → `filter(Boolean)`) — вместе с ценой
  и без следа в списке пропусков;
- при отсутствующем `supportCount` смета брала 1, а свод — число коробок.
Смета теперь отдаёт **структурный состав**, печатная строка собирается из него же.
Текст КП не изменился ни на символ.

**4. Починен баг конвертера, живший с начала проекта.** Колонка `«Подгруппы »` объявлена
с пробелом (как в файле), но заголовки тримятся перед поиском — совпадения не было **никогда**,
поле было `null` у всех 2146 позиций (в файле заполнено у 161). Поиск переведён на
нормализованные заголовки — расхождение по пробелам стало невозможным как класс.
Та же ловушка была в регулярке: `` не работает с кириллицей (`\w` — только латиница),
поэтому `/^Клавиша/` не находил ничего.
Заодно выведен надёжный признак «клавиша или голый механизм» (**18 механизмов, 34 клавиши**)
вместо эвристики по слову в названии, которая ставила `categoryId=900` всему, где есть
«клавиш», «накладк», «крышк» или «винт».

**Про качество проверки.** Модуль групп света потребовал шести раундов ревью, и каждый находил
дефект одного класса — **результат зависел от порядка входных данных**: «первые два места»
брались по входу; двухклавишный пост схлопывался в одно место; компаратор был нетранзитивен
(9 < 10, 10 < "5", "5" < 9); подпись серий склеивалась через разделитель (36.94 € против 48.32 €);
кэш возвращал написание, пришедшее первым (**40.52 € против 0 €**). Один и тот же проект обязан
давать один и тот же расчёт. Итог: 92 теста плюс проверка свойством — 19 200 проектов
с перемешиванием, 76 800 сравнений, ноль расхождений.
Инвариант свода проверен на **966 реальных именах** каталога с запятой: старый разбор превращал
«Розетка … немецкий стандарт, карбон матовый» в пять позиций, где «карбон матовый» становился
отдельной позицией заказа.

**КОНТРАКТ ДЛЯ ИНТЕГРАЦИИ модуля групп света** (нарушение = молчаливая ошибка в деньгах):
- **позиция клавиши в посте обязательна** — без неё дубли не опознаются: одно место, поданное
  трижды, даёт N=3 и **93.91 € вместо 20.26 €**;
- **номера групп передавать строками** — число `4.10` в JS это `4.1`, а у заказчика на планах
  номера именно такого вида («4.1», «4.2»);
- `keyIndex` читается только в десятичной записи; `deps.findMechanism` обязан возвращать
  изделие с артикулом; серии приходят в написании места, регистр не приведён;
- интерфейсу нужен текст для причины пробела `GAPS.NO_CODE`.

**Открыто:** интеграция групп света в UI (C8, в работе вместе с полноэкранным конструктором),
затем B4+B5 (двойной клик, правка копий поста), E15 поиск по артикулу, D10 выбор столбцов,
E13 фиксация коллекции за помещением. Письмо заказчику выросло до **9 вопросов**, четыре
блокирующих — добавились суппорт `22673.2.*` (противоречие внутри данных заказчика) и реле.

### 2026-08-26 — Разбор встречи 24.08, два фикса сметы (НЕ ЗАКОММИЧЕНО, есть открытый дефект)

> **ЧИТАТЬ ПЕРВЫМ ДЕЛОМ В СЛЕДУЮЩЕЙ СЕССИИ.** В рабочем дереве ~19 изменённых файлов,
> **ничего не закоммичено**, `HEAD` = `7464bf8`. Тесты: **330 pass, 0 fail** (было 281).

**Разбор материалов встречи 24.08** (3 записи ~70 мин транскрибированы через `faster-whisper`,
опросник с ответами заказчика, номенклатура, презентация, эталонные расчёты) —
итог в `docs/итоги-встречи-2026-08-24.md`, письмо заказчику в
`docs/письмо-заказчику-вопросы-2026-08-26.txt`. Оба не в git.

**Ключевое по продукту:** заказчик — **ИП Пургин, бренд EPG «Энергия Позитива Груп»**
(не ЦентрСвет), нумерация КП `EPG-2026-0001`. Продукт — **многопользовательский веб-сервис
на продажу по лицензии**, уровни доступа демо/менеджер/профи. **Битрикс перестал быть
обязательным.** **ML-распознавание планов понижено в приоритете** — стены нужны только для
привязки коллекций к помещениям. Приоритет заказчика: сначала механика подбора, потом визуал.

**Что сделано в коде (3 раунда через Workflow, код писали сабагенты):**
1. **Суппорт × число постов.** Было: `postCost` умножал на количество только коробку,
   суппорт входил 1 раз → смета занижена на каждом немецко-французском посте 2+2.
   Заказчик заметил это вживую на записи. Добавлен `supportCount` в `postComposition()`,
   проведён в смету, КП, лист монтажника, взрыв-схему, панель конструктора.
2. **Подбор суппорта/коробки по данным номенклатуры** вместо эвристики. Колонки
   `Принцип обработки` (`principle`) и `Модульность для коробки` (`boxModularity`) читались
   конвертером, но **не пробрасывались в каталог** — отсюда пустой суппорт у центральных
   накладок. Проброшены полностью: `principle` 449/449, `boxModularity` 275/275.
   `NO_SUPPORT` теперь пишет «не требуется» вместо пустой строки.
3. **Флаг `supportAssumed`** — пометка «(предположительно)» у суппортов, подобранных
   не по прямому указанию заказчика. Стоит на 36 накладках из 1631 (`09671.*`, `09679.*`,
   `22673.1.*`, `22683.1.*`). Решение владельца: подставлять артикул, но помечать.
4. `buildFittings()` вынесен из `app.js` в `installSheet.js` и покрыт тестами.

**✅ ИСПРАВЛЕНО В ЭТОЙ ЖЕ СЕССИИ (раунд 4) — приоритет источников при подборе суппорта.**
В раунде 3 я (Claude) дал сабагенту неверное указание: «восстановить приоритет общего правила
602/603». Ревью назвало регрессом переход с эвристики на данные заказчика, сравнив с `HEAD`, —
но на `HEAD` была эвристика, а не истина. Поймала состязательная проверка: восстановленный
приоритет выбирал суппорт, прямо запрещённый ремаркой номенклатуры.

**Разобрано по исходной номенклатуре:** заказчик разметил пары «накладка → суппорт» адресно,
через `Принцип обработки` + `Модульность для коробки`. Ровно 7 суппортов несут `principle`:
`14601`/`19601`/`21601.0` (1M_CENTRAL, boxMod=2 — для `14641.*`, `19641.*`, `22672.1.*`, `22682.1.*`)
и `09606`/`14612`/`19612`/`21612` (2M_CENTRAL, boxMod=3 — для `09672.*`, `14652.*`, `19652.*`,
`19672.*`, `22673.2.*`, `22683.2.*`). Кажущееся противоречие «1М-суппорт в 2М-коробку» снято:
у `14601`/`19601`/`21601.0` стоит `Модульность для коробки = 2`.

**Итоговый порядок в `resolveSupport()` (`js/postfit.js`):**
1. адресная пара (`principle` + `boxModularity`) — конкретное указание заказчика;
2. общее правило 602/603 по коробке — там, где пары нет (в частности `09661.*`, чья ремарка
   даёт ДВА варианта, и выбор между ними идёт по типу стены);
3. общий подбор по модульности.

Множества шагов 1 и 2 не пересекаются: ни один суппорт с `principle` не имеет кода 602/603,
а у `09602.1`/`09603.1` `principle` отсутствует — поэтому перестановка ничего у `09661.*` не отобрала.

**Замер (1631 накладка × 2 типа стены, три версии кода):** 87 накладок вернулись к правильному
суппорту (`14641.*`→`14601`, `19641.*`→`19601`, `22672.1.*`/`22682.1.*`→`21601.0`), и ничего
кроме них не сдвинулось. `09661.*` сохранили `09602.1`/`09603.1`. Потерь суппорта нет
(состязательная проверка: 176 148 сравнений, 0 потерь). Тест, закреплявший ошибку, заменён на
противоположный по смыслу и проверен на фальсификацию — он краснеет при возврате старого порядка.


**Второй high из ревью:** критерий `assumed` выведен из того, какое правило сработало,
а не из наличия артикула в ремарке — как заявлено заказчику в письме. Причина: колонка
`Описание особенностей элемента` (`COL.note`) читается конвертером, но **не выгружается**
в `js/catalog-vimar-attrs.js`. Чтобы критерий совпал с обещанным, надо выгрузить `note`
(или производный «названный артикул суппорта») в `build-catalog-attrs.mjs`.

**Мелочи из ревью (low):** в комментариях `js/posts.js:129` и `js/estimate.js:63` перечень
семейств с флагом неполон — пропущен `22683.1.*` (число 36 верное); `js/app.js:1299` в причине
«суппорт не подобран» называет модульность накладки вместо `boxModularity`;
`js/postfit.js:186` — разорванный комментарий.

**Замеры (стенд в scratchpad, 1631 накладка × 2 типа стены):** честные пробелы
(суппорт нужен, но не найден) `HEAD` 60 → сейчас 36; оставшиеся 36 — семейства
`14690`/`14691`/`19660`/`19661`, в этих сериях нет планки нужной модульности.
Суппорт чужой серии — **0 случаев**.

**Требует внимания владельца:**
- **Фикс суппорта повышает смету** на (N−1) суппортов за каждый немецко-французский пост.
  Ранее выставленные КП разойдутся с новыми — заказчика лучше предупредить заранее.
- **Ветка `client` устарела** (`a66122e`, 01.08). Заказчик на встрече сказал «взрыв-схемы
  ещё нету» — она есть с `7464bf8`. Он тестирует старое и шлёт замечания по закрытым вопросам.
  Обновлять только по прямому слову владельца (force-push, процедура в разделе 7).
- **Смежный риск, не трогали:** в `findSupport()` целевая модульность берётся по ёмкости всей
  накладки, а не одного поста. На текущих сериях безвредно, но в серии с несколькими
  603-суппортами разной модульности даст завышение. Отдельная задача.
- `14652.*` (34 шт.) раньше получали `14507` — **суппорт накладного монтажа**; теперь `14612`.
  Похоже, старое поведение было ошибкой, но стоит сверить с заказчиком.

**✅ D9 СДЕЛАНО — план с бирками номеров постов в документах.**
Автонумерация постов уже работала (`EPPosts.nextPostNumber` при размещении, кнопка
«Перенумеровать» по (y,x)). Не хватало другого: **самого плана в документах не было вообще** —
в таблице «Пост №7» есть, а найти его на объекте нельзя. У заказчика это отдельная страница
эталонного «Расчёта ЭУИ»: титул → план с бирками → таблица постов.

Новый чистый модуль **`js/planLabels.js`** (`layout()` — геометрия числами, `buildHtml()` — HTML
только с инлайн-стилями, оба под тестами, 14 тестов в `tests/planLabels.test.js`). Блок
встраивается в КП (`js/offerPdf.js`, перед «Раскладкой постов») и в лист монтажника всего
проекта (`js/installSheet.js`, перед карточками). Лист ОДНОГО поста из конструктора блок не
получает — чужие номера там мешают.

Решённые тонкости:
- **Координаты.** Зум/панорама не участвуют (это CSS-трансформация `#canvas`, в `post.x/y` её нет).
  Компенсировать надо **леттербокс** подложки (`object-fit:contain`) и смещение иконки
  (`POST_ICON_HALF=12`, клик кладётся как `x-12,y-12`). Проверено в браузере рендером документа
  в iframe и замером `getBoundingClientRect`: расхождение бирок с экраном **0.00 px**.
- **Печать.** Бирка — белый кружок с синей рамкой и тёмным номером, а не синяя заливка: Chrome
  по умолчанию не печатает фоны, синий кружок ушёл бы в PDF невидимым.
- **Тяжёлая подложка.** `planImageForDoc()` пережимает в JPEG: живой план 16.39 МБ → 1.22 МБ.
- **Пост за краем подложки** не обрезается — кадр расширяется до объединения плана и бирок.

Поправлено мной после ревью: порог сжатия шёл через `||`, из-за чего подложка с разрешением
≤1800 px не сжималась **никогда, при любом весе** (детальный план 1200×900 на 15 МБ попадал в
документ целиком) — заменено на `&&` плюс `k=min(1,…)`, чтобы мелкую картинку не растягивать;
подпись под планом в КП ссылалась на «спецификацию», хотя номера там в «Раскладке постов».
Добавлены 4 контрактных теста, что план реально печатается и стоит ПЕРЕД таблицами, — вставку
теперь нельзя молча удалить или переставить.

**Тесты: 349 pass, 0 fail.**

**Известные мелочи (не чинил):** бирка 22×22 px обрежет 4-значный номер (проект на 1000+ постов);
расширение кадра под улетевшую бирку ничем не ограничено — один пост, утащенный далеко за
подложку, ужмёт чертёж; высота мирового бокса `#canvas` меняется от переноса строки в
`.canvas-footer` (517→533 px), из-за чего леттербокс подложки чуть съезжает — **это давний
дефект приложения, не блока плана** (документ показывает ровно то же, что экран).

**Дальше по плану (согласовано с владельцем):** D9 сделано (выше) →
C6–C8 группы света и схемы электрики → D10 выбор столбцов в документах → E13 фиксация
коллекции за помещением. Полный список — `docs/итоги-встречи-2026-08-24.md`, §8.

### 2026-08-05 — Деплой превью, взрыв-схема листа монтажника с фото, PLAN.md актуализирован
- **Зачем:** владелец захотел коммитить/пушить проект и дальше, но чтобы заказчик видел только
  согласованную версию, а тимлид — реальный прогресс; отдельно попросил взрыв-схему сборки поста
  в лист монтажника (по образцу иллюстраций из каталогов VIMAR) и актуализацию `PLAN.md`.
- **Что сделано:**
  1. **Деплой на Cloudflare Pages** — см. раздел 7 выше, подробности процедуры там, не дублирую.
     Подключение GitHub↔Cloudflare через веб-интерфейс глючило (петля OAuth), почти всю боль занял
     этот шаг, а не код.
  2. **Фикс раскладки постов** (коммит `96071a8`) — `EPPosts.distributePosts` использовал жадный
     next-fit и мог поставить многомодульный механизм верхом на импост, давая ложную
     «Несовместимое сочетание». Заменено на полный перебор с возвратом (`packAll` в `js/posts.js`);
     если валидная раскладка существует — она находится, конструктор сам переставляет порядок
     слотов под неё. 263→274 теста (позже ещё выросло, см. п.3).
  3. **Взрыв-схема поста** (`js/explodedView.js`, коммит `7464bf8`) — итеративно, по правкам
     владельца после каждого показа результата:
     - v1: разнесённая диаграмма (накладка/механизмы/суппорт/коробка) с иконками-глифами и
       выносными линиями к подписи «Название — Артикул».
     - v2: заменил иконки на настоящие фото товаров через `EPCatalog.productImage()` — выяснилось,
       что фото есть у бОльшей части каталога (используются в панели товаров и в конструкторе), а
       не только у накладок, как предполагалось изначально при постановке задачи агенту — это была
       моя ошибка в скоупинге, пришлось переоткрывать задачу.
     - **Фото для 10 монтажных коробок** (`V71306`…`V71720`, у которых не было фото ни на vimar.ru,
       ни в официальном индексе) — найдены вручную на **vimar.com** (не `.ru`!), подключены через
       новый `tools/data/image-overrides.json` (применяется в `build-catalog.mjs` тем же способом,
       что `kind-overrides.json` — переживает пересборку каталога из нового прайса).
     - v3: владелец пожаловался на визуальную вложенность иконок (диагональный «шампур» делал
       мелкие иконки похожими на вложенные) → убрана диагональ, единая вертикальная ось.
     - v4: владелец попросил горизонтальную раскладку и иконки крупнее ×2 → полная перекомпоновка.
     - v5: владелец попросил, чтобы механизмы одного поста шли вертикальным стеком внутри
       горизонтального ряда (не отдельными карточками наравне с накладкой/суппортом/коробкой) →
       группировка `spec.parts` по `role` внутри `explodedView.js`. Итог — 4 позиции в ряду
       (накладка / стек механизмов / суппорт / коробка), протестировано и проверено в браузере.
     - Каждую итерацию после возврата сабагента (`ep-dev`) проверял САМ: `npm test`, и вживую в
       браузере через локальный сервер (`.claude/launch.json`, порт 8420) — собирал тот же тестовый
       пост (накладка `09673.03` + механизмы `09001.0.250`/`09208.C.CM`, стандарт то ГКЛ, то бетон)
       через реальный UI конструктора, патчил `window.open` в консоли, чтобы перехватить HTML
       листа монтажника вместо реального всплывающего окна (в автоматизированном браузере попапы
       блокируются молча) — так подтверждал каждую правку не на слово агента, а сам.
  4. **`PLAN.md` актуализирован** сабагентом по факту кода (не по commit-сообщениям): 4 пункта
     закрыты, 4 переведены в «частично» с пояснением, что осталось; добавлен раздел «Открытый
     вопрос к заказчику» про суппорт NEVE UP.
  5. **Рыночное исследование аналогов** (VIMAR/Legrand/Gira/JUNG-конфигураторы, CubiCasa/Planner5D,
     промышленный CAD, SaaS-сметы, AR-инструменты) — вывод: прямого аналога, сочетающего локальное
     браузерное распознавание плана + проверку совместимости по реальному прайсу дилера + PDF КП,
     на рынке не нашлось. Отчёт **только в scratchpad сессии**, не в репозитории — см. раздел 6.
  6. **Ветка `client` дважды пересобрана** по прямому указанию владельца — сначала на `d02e9bf`
     (до ответов заказчика), затем на `0a1d7e4`, наконец остановились на `a66122e` (владелец сам
     назвал коммит после того, как я показал список и объяснил, что изменилось в каждом). См.
     раздел 7 — процедура пересборки и текущее состояние.
  7. Подготовлен список вопросов к встрече с заказчиком 06.08 — `docs/вопросы-заказчику-встреча-
     2026-08-06.txt` (не в git).
- **Как сделано:** сам код почти целиком писали сабагенты `ep-dev` (правило проекта — Claude
  только оркестратор, см. память `role-orchestrator-only`), я — ставил задачи, проверял диффы и
  тесты, гонял в браузере, коммитил и пушил по прямому слову владельца.
- **Проблемы:**
  - Веб-интерфейс Cloudflare Pages для подключения GitHub несколько раз зацикливался (петля OAuth)
    — не чинится с первого раза, помогло полное удаление приложения в GitHub
    (`github.com/settings/installations`) и переустановка с нуля.
  - При смене веток туда-обратно (`main` ↔ `client`) несколько раз `git checkout` упирался в
    untracked-конфликты (`.idea/`, `chertez/`, `vendor/`, старые доки) — потому что `.gitignore`
    в старых снимках `client` не исключает то, что исключает актуальный `.gitignore` в `main`.
    Лечится `git stash push -u -- <конкретные пути>` (НЕ `git clean`, НЕ `rm -rf`) — обратимо,
    можно посмотреть/вернуть из стэша в любой момент. Процедура теперь описана в разделе 7.
  - Я неверно предположил в первой версии задачи про взрыв-схему, что фото есть только у накладок
    — потратил лишний цикл на исправление. В следующий раз перед постановкой задачи агенту стоит
    сначала быстро проверить в браузере (`window.EPCatalog`, `EP_VIMAR_CATALOG`), что реально
    доступно в данных, а не полагаться на память о более ранних коммитах.
- **Открыто на конец сессии:** суппорт NEVE UP (см. раздел 6 выше и `docs/записка-заказчику-
  подбор.txt`) и остальные вопросы встречи 06.08 — ждут заказчика.

### 2026-07-26 — Точная трассировка стен: гибрид «YOLO-зона + пиксели чертежа» (В РАБОТЕ)
- **Зачем:** заказчик подтвердил, что главное требование — **точно повторять стены и перегородки
  по рисунку и не принимать мебель за стены** — не выполнено, а ручная правка контуров его не
  заменяет. Ни классический CV (не отличает стену от дивана), ни YOLO сам по себе (даёт грубые
  прямоугольники вместо геометрии) требование не закрывают.
- **Найденный подход (без новых моделей и без Python):** YOLO определяет, **где** стены
  (мебель он не размечает по построению), а геометрия берётся из **реальных тёмных пикселей
  чертежа внутри этих зон**. Формально: `стены = Otsu(тёмное) ∩ (боксы Wall/Curtain Wall/Column + pad)`,
  затем отсев мелких компонент. Мебель выпадает, потому что лежит вне зон стен.
- **Стенд:** `seg-preview/wall-trace-test.html` (в .gitignore) — грузит opencv.js + floorplanML.js,
  прогоняет план, рисует серый чертёж + красные стены + синие проёмы, отдаёт метрики.
  Открывается на `http://localhost:8420/seg-preview/wall-trace-test.html`.
- **Результат на 5 планах `chertez/`:** мебель, сантехника, текст и размерные линии отсекаются —
  отброшено 37–73% тёмных пикселей. На `639` (где классика даёт 0 комнат) стены легли по месту.
- **Ключевая находка — порог детекции.** При `conf=0.20` YOLO находит 21 стену и покрытие рваное
  (левая/правая/нижние стены выпадают целиком). При `conf=0.05` — 42 стены, контур почти сплошной.
  Порог по умолчанию в приложении (0.22) для этой задачи **слишком высокий**.
- **Добор пропущенных стен — перебрано три варианта, выбран третий:**
  1. *Связность* (принимать целиком связную компоненту, задетую подтверждённой стеной) —
     **провал на обмерных планах**: там стены, размерные линии, сантехника и рамка штампа
     физически соединены, компонента утягивает весь лист (на `00b` отброшено лишь 17,6%).
  2. *Фильтр по толщине* (distance transform, полоса вокруг медианной полутолщины стены) —
     **тоже провал на `00b`**: стена там нарисована **двумя тонкими параллельными линиями**,
     а не сплошной полосой, поэтому её штрих неотличим по толщине от размерных линий.
     Важный вывод: толщина работает только на планах с залитыми стенами.
  3. *Продление зоны вдоль оси стены* (для каждого бокса Wall определяем ориентацию по
     сторонам и продлеваем его в свою сторону на ~5% размера листа; кандидаты = тёмное ∩
     продлённая зона, принимаем куски, связанные с подтверждённой стеной) — **рабочий
     компромисс**. Пропуски вдоль стен закрываются, а штамп и экспликация в стороне от осей
     в зону не попадают.
- **Метрики (доля тёмного, отброшенная как «не стена»):** `639` 22,6→10,8% при добранных
  +18k px (покрытие выросло); `00b` 17,6→41,5% (мусор отсекается лучше); `scale_1200` 22,3%.
- **Порог детекции решает многое:** штатный `conf=0.22` даёт на `639` 21 стену и рваный контур,
  `conf=0.05` — 42–49 стен и почти сплошной. Для трассировки нужен низкий порог.
- **Остаточный дефект:** на обмерных планах сантехника (ванна, унитаз, раковина) прижата к
  стенам, попадает в продлённую зону и всё ещё отмечается как стена. Ни зоной, ни толщиной
  не лечится — нужен семантический признак (детекция сантехники отдельным классом либо
  распознавание стен как пары параллельных линий).
- **Конвейер собран целиком (2026-07-26) — комнаты определяются.** Шаги после обводки стен:
  1. *Замыкание разрывов* — направленное `MORPH_CLOSE` отдельно длинным горизонтальным и
     длинным вертикальным ядром. Мостит разрывы вдоль оси стены и не утолщает её поперёк,
     в отличие от квадратного ядра.
  2. *Перемычки в проёмах* — бокс двери шире стены и, залитый целиком, торчит внутрь комнаты.
     Поэтому рисуем полосу толщиной со стену по её осевой: направление определяем по тому,
     с каких сторон от бокса есть стеновые пиксели, центр — по их медиане. Если соседних
     стен не нашли, бокс запечатывается целиком (дыра хуже лишнего прямоугольника).
  3. *Watershed* по distance transform свободного пространства. Обязателен: часть проёмов —
     арки без полотна, YOLO их не находит, одних перемычек мало. На `639` без watershed
     894k px сливались в одну область.
  4. *Слияние регионов* — watershed режет по каждому узкому месту и дробит комнату. Если
     доля общей границы двух областей, лежащая на барьере, меньше 50% — разрез искусственный,
     области объединяются (union-find). На `639` 9 комнат → 4.
  5. *Отсечение «улицы»* — заливка от края листа по свободным пикселям. **Порядок критичен:**
     заливка должна идти ДО обрезки по габариту здания, иначе край листа уже обнулён и
     стартовать неоткуда (потратил на это лишний прогон).
  6. *Защита от протечки* — если наружная стена разорвана, заливка уходит внутрь и съедает
     все комнаты (на `639` осталась 1). Признак: внутри габарита осталось <35% свободного
     пространства → откат к варианту без отсечения улицы.
- **Результат по эталонам:** `scale_1200` — **5 комнат** (эталон из этого же файла: 5, все
  разделены ✓); `_page-0006` — 8 (эталон 7); `00b` — 6 (было 10–13 у классического CV);
  `639` — 4, но сработала защита от протечки (наружный контур разорван).
- **ПЕРЕНЕСЕНО В ПРИЛОЖЕНИЕ (2026-07-26).** Конвейер заменил прежнюю (нерабочую) реализацию
  `EPFloorplanML.segmentRooms` в `js/floorplanML.js`; полигонизация — существующая связка
  `findContours` → `approxPolyDP` → `EPRoomSeg.orthogonalize` по линиям стен. В UI добавлена
  кнопка **«Определить комнаты (ML)»** (`detectRoomsMlBtn`, обработчик `detectRoomsML` в app.js),
  рядом с прежней OpenCV-кнопкой. Ручные контуры переживают пересчёт, имена не дублируются —
  та же логика, что и в OpenCV-режиме.
- **Порог `CLASS_CONF.Wall` снижен 0.12 → 0.05** и `BASE_CONF` 0.10 → 0.05: боксы стен служат
  лишь зоной внимания, точность даёт растр, поэтому лишние срабатывания не вредят, а пропуски
  рвут контур. На кнопку «Распознать элементы» это не влияет — она зовёт `detect` с conf 0.22.
- **Проверено в приложении** на `chertez/1677388_900_600.png`: 7 комнат, контуры по стенам,
  ROI на весь план (667k px), стен 152 885 px, ошибок в консоли нет.
- **Грабли при переносе:** переписывая `DEFAULTS`, удалил параметр `clusterGap`, которым
  пользуется оставшийся код кластеризации боксов. Сравнение `<= undefined` всегда ложно →
  каждый бокс стал отдельной «сетью конструктива» → ROI схлопнулся в 115×107 px, стен 0,
  комнат 3 крошечных. Диагностировалось только по отладочному полю `debug` в результате
  `segmentRooms` (оставлено в коде: `roiArea`, `roi`, `wallPx`, `barrierPx`, `labels`).
- **Разрыв наружной стены починен (2026-07-26).** Вместо отката при протечке барьер
  временно «заклеивается» утолщением, и подбирается минимальное из ряда `0/5/11/21/35`,
  при котором заливка перестаёт течь внутрь. Утолщение живёт **только** внутри поиска
  «улицы» — геометрия стен и контуры комнат считаются по неутолщённому барьеру, поэтому
  не искажаются. `sealK` и `streetCut` видны в `debug`.
- **Итог по эталонам (leakGuard 0.35):** `_page-0006` — **7 комнат (эталон 7 ✓)**,
  `scale_1200` — **5 (эталон 5 ✓)**, оба без утолщения (`sealK=0`); `1677388` — 5
  (`sealK=11`); `639` — 2 (`sealK=21`); `00b` — 3. Откат не срабатывает ни на одном плане.
- **Побочный эффект на `639`:** улица теперь отсекается корректно (раньше её кусок в
  ~250k px числился комнатой), но помещений находится всего 2 — заливка при `sealK=21`
  всё же съедает часть интерьера. Недосегментация, а не ложные комнаты.
- **`leakGuard` подбирался:** 0.55 пробовал — хуже (на `00b` 6→3, на `639` заклейка
  переставала срабатывать). Оставлено 0.35.
- **⚠️ НЕ ПРОВЕРЕНО (2026-07-26): точечная заклейка выпуклой оболочкой.** В список попыток
  отсечения улицы добавлен режим `"hull"` — обводка выпуклой оболочки стеновой сети
  (`findContours` → `convexHull` → `drawContours` толщиной со стену). Идея: замкнуть разрыв
  наружного контура, не съедая интерьер вдоль каждой стены, как это делает утолщение всего
  барьера. Порядок попыток: `none → hull → 5 → 11 → 21 → 35`, берётся первая, прошедшая
  `leakGuard`. Режим виден в `debug.sealMode`.
  **Проверить не удалось:** вкладка браузера была придушена throttling'ом (панель не
  отображалась), ML-прогон не доходил даже до декодирования картинки. Изменение
  безопасно по построению — это дополнительная, более бережная попытка перед прежними,
  под тем же порогом `leakGuard`, — но фактические числа по планам НЕ измерены.
  **Первое дело следующей сессии:** прогнать 5 планов из `chertez/` и сверить с таблицей
  выше (`_page-0006` 7, `scale_1200` 5, `1677388` 5, `639` 2, `00b` 3). Ожидание: на `639`
  комнат должно стать больше 2 при `sealMode="hull"`. Если хуже — убрать `"hull"` из
  `sealAttempts`, это одна строка.
- **Что осталось:** (1) недосегментация на `639` и `00b` — вероятный путь: искать разрыв
  наружного контура точечно, а не глушить утолщением всего барьера; (2) параметры привязаны
  к `analysisW=1000` — проверить на очень крупных чертежах; (3) сантехника вплотную к стенам
  на обмерных планах всё ещё попадает в стены (нужен семантический признак); (4) стенд
  `seg-preview/wall-trace-test.html` оставлен как площадка (в .gitignore).
- **Проблемы по дороге:** нативного Python на машине нет (только заглушка Windows Store), поэтому
  пивот на CubiCasa5k отпадал — конвертировать модель в ONNX нечем; папки `.tmp-cubicasa5k` и
  `.tmp-floorplan-yolo` с прошлых сессий удалены. В стенде поймал зависание на `await window.cv`:
  модуль OpenCV — «thenable», резолвящийся сам в себя, `await` виснет навсегда; работает только
  `.then()`. Ещё: у `cv.Mat` поле называется `data32S`, а не `intData`.

### 2026-07-24 — Приоритет 1: правка контуров комнат, масштаб и площадь + фикс загрузки OpenCV
- **Что сделано:**
  1. **Ручная правка полигонов комнат** (`PLAN-segmentation.md` 3.5, частично) — новый инструмент
     «Правка комнат»: перетаскивание вершин, добавление вершины кликом по середине ребра,
     Alt+клик — удаление (не менее трёх вершин). Правки переживают повторное авто-определение.
  2. **Масштаб плана и площадь в м²** (`PLAN.md` 3.3/3.4, Фаза 4) — кнопка «Задать масштаб»,
     эталонный отрезок двумя кликами, площадь комнат по формуле шнурков, ручной ввод приоритетнее.
  3. **Починена ленивая загрузка OpenCV.js** — до этого «Определить комнаты» не работала вообще.
- **Как сделано:** `js/app.js` — `polygonAreaPx`/`roomAreaM2`/`roomDisplayArea`, `renderVertexHandles`
  + `dragVertex`, блок «Масштаб плана» (`renderScaleRuler`, `addScalePoint`, `clearScale`),
  новые ветки в `canvas.onclick` и `setTool`. `index.html` — кнопки, слой `#scaleSvg`, модалка
  масштаба. `css/styles.css` — стили маркеров, линейки, подсказок. Масштаб пишется в
  `projectSnapshot`.
- **Проблемы:**
  - **Главное:** «Определить комнаты» была сломана — `loadOpenCv()` резолвился, не дождавшись
    промиса модуля, из-за чего `cv.Mat` оставался `undefined` и `EPRoomSeg.segment()` падал с
    «OpenCV.js (cv) не загружен». Дефект существовал до этой задачи (код не менялся), но блокировал
    приоритет 1 целиком — полигонов не появлялось. Фикс: дожидаться `window.cv` и подменять его
    разрешённым модулем. После фикса на `chertez/1677388_900_600.png` — 6 комнат.
  - Повторный запуск определения давал дубли имён («Комната 3» ручная + «Комната 3» новая) —
    исправлено нумерацией от максимума существующих.
  - Центроид вогнутого полигона (16 вершин) лежит вне контура — клик по нему комнату не выбирает.
    На саму фичу не влияет (выбор работает по любой внутренней точке), но для будущего
    авторазмещения подписей стоит считать точку внутри полигона, а не центроид.
  - Скриншоты браузера в этой сессии были недоступны (панель не отображалась), поэтому проверка
    шла через чтение DOM и события: площади сверены с независимым пересчётом по SVG-координатам.
- **Проверено:** масштаб 200 px = 4 м → 50 px/м; площади 6 комнат совпали с контрольным расчётом;
  перетаскивание вершины 0,7 → 1,7 м²; добавление 4→5 и удаление 5→4 вершин; защита «минимум 3»;
  ручной контур пережил повторное определение, дублей имён — 0; ошибок в консоли нет.

### 2026-07-24 — Создание handoff-документа
- **Что сделано:** составлен этот файл `HANDOFF.md` как входной документ проекта; добавлено правило
  в `../CLAUDE.md` (корень `electro/`), чтобы в начале каждой сессии Claude читал этот файл;
  зафиксирован указатель в auto-memory.
- **Как сделано:** изучены memory-файлы проекта, `PLAN.md`, `PLAN-segmentation.md`,
  `docs/WORK_REPORT_2026-07-23.md`, `index.html`, текущий `git status`/`git diff`
  (в т.ч. изменение `local/.../ProductRepository.php`). Содержимое сведено в разделы 1–6.
- **Проблемы:** значительный объём работы велся вне git-индекса (новые файлы `js/*.js`, `vendor/*`
  не закоммичены) — статус собран из рабочего дерева, а не из истории коммитов. Часть ML-экспериментов
  гонялась параллельно через OpenAI Codex CLI в том же дереве (`.tmp-*`), в основной репозиторий не входит.
